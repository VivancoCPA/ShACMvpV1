## 1. Preparar el entorno local

- [x] 1.1 Confirmar `docker-compose up -d` en `ShcMvpEndPoint/` con `shac-postgres` sano — confirmado, `shac-postgres` estaba `Up 8 days (healthy)`.
- [x] 1.2 Confirmar `dotnet run` levanta sin errores contra Postgres dev, anotar el puerto real — confirmado, `http://localhost:5072` (perfil `http`), responde 401 en `/api/nonconformities` sin token. Arranque lento (~igual que lo ya documentado en `cutover-incidentes` 1.2): `DevSeedAreasExtensions` re-siembra áreas por cada empresa dev existente, generando decenas de miles de líneas de log — no bloqueante para este change.
- [x] 1.3 Confirmar que los usuarios QA multi-empresa reutilizables de `cutover-auth`/`cutover-catalogos`/`cutover-incidentes` siguen sirviendo para probar No Conformidades — confirmado por login directo: `multi.empresa.qa@shac.dev`/`QaTest_2026!` sigue siendo mono-empresa (login directo sin selección), rol `JEFE_CALIDAD_SYST` en "QA Empresa Beta S.A.C." — cubre `canAnular`/`canCerrarAC`/`canAsignarAC`/`canDelete`/`canRestore` (`ncPermissions.ts`). `mono.empresa.qa@shac.dev`/`QaTest_2026!` sigue siendo multi-empresa (requiere selección, 4 empresas disponibles), rol `SUPERVISOR` en "Minera Smoke Test SAC" — cubre `canEdit`/`canIniciarInvestigacion`/`canAsignarAC`/`canCrearQE`. Mismo drift ya documentado en `cutover-incidentes`/`cutover-documentos` (mono↔multi con roles invertidos respecto al nombre del fixture) — no bloqueante, ambos roles necesarios están cubiertos entre los dos usuarios.

## 2. Confirmar que no hay mismatch de contrato (re-verificación previa a tocar el entorno)

- [x] 2.1 Releer `ActualizarNoConformidadCommand.cs`/`ActualizarNoConformidadHandler.cs` y confirmar que el whitelist de campos PATCH sigue coincidiendo con `updateNC.schema.ts` — confirmado sin cambios (`git diff --stat` sobre `Features/NoConformidades` desde la verificación previa de esta sesión da vacío).
- [x] 2.2 Releer `EndpointExtensions.cs` y confirmar que las 10 rutas de No Conformidades siguen registradas y coinciden con `nonconformities.api.ts` — confirmado sin cambios (mismo `git diff --stat` vacío, incluye `EndpointExtensions.cs` y `features/nonconformities`).
- [x] 2.3 Sin discrepancias detectadas — no aplica documentar nada nuevo en `design.md`.

## 3. Apuntar el frontend al backend real

- [x] 3.1 Editado `shc-controldoc/.env.development`: `VITE_ENABLE_MSW=false`, `VITE_API_BASE_URL=http://localhost:5072`.
- [x] 3.2 `npm run dev` levanta sin errores (`vite v8.0.16 ready`, sirviendo en `http://localhost:5173/`, HTTP 200). **Nota de metodología (esta sesión, distinto de `cutover-incidentes`/`cutover-documentos`): no hay herramienta de automatización de navegador disponible en este entorno** (sin Playwright/similar). Se verifica el contrato completo por API directa (`curl` contra `http://localhost:5072`, JWT real obtenido por login) para todas las secciones 4-6, en vez de clic en la UI — mismo nivel de rigor que ya se usaba para los escenarios sin punto de entrada en la UI (transición de estado), extendido aquí a todo el módulo por la limitación de la herramienta, no por limitación del contrato. Login confirmado contra el backend real: `multi.empresa.qa@shac.dev` (mono-empresa, rol `JEFE_CALIDAD_SYST`, empresa "QA Empresa Beta S.A.C.") y `mono.empresa.qa@shac.dev` (multi-empresa, rol `SUPERVISOR` en "Minera Smoke Test SAC" tras seleccionar empresa) devuelven `accessToken` JWT válido.

## 4. Verificar CRUD, anulación y acciones correctivas (frontend-no-conformidades-cutover-verification)

- [x] 4.1 Creación de No Conformidad vía API directa (`POST /api/nonconformities`, contrato de `NCForm`) — confirmado: `numero: "NC-CAL-2026-003"` (formato `NC-[DOMINIO_ABBR]-YYYY-NNN` correcto), `estado: 'ABIERTA'`, `documentosVinculados: []`.
- [x] 4.2 Filtros reales contra `GET /api/nonconformities`: `estado=ABIERTA`, `dominio=CALIDAD&severidad=MEDIA`, `search` (confirmado que solo matchea `numero`/`descripcion`, no `titulo` — coincide con `be-no-conformidades-api` spec.md "Búsqueda por texto"; mi primer intento de search sobre texto de `titulo` devolvió `[]` correctamente, no es un bug), `showDeleted` (confirmado explícitamente: `false`/default excluye una NC recién eliminada, `true` la incluye con `deletedAt` seteado).
- [x] 4.3 Edición parcial (`PATCH /:id` con `responsableInvestigacionId`, `correccion`, `causaRaiz`) — confirmado: los 3 campos persistidos y 3 entradas `CAMPO_EDITADO` en el audit trail con `campoModificado` correcto por cada uno.
- [x] 4.4 Anulación con justificación (`POST /:id/anular`) — confirmado: `estado: 'ANULADA'`, `justificacionAnulacion` persistida, audit trail `ANULADA` con `estadoAnterior: 'ABIERTA'`/`estadoNuevo: 'ANULADA'`. **Hallazgo de tooling, no de la app** (diagnosticado con log del backend, no asumido): un primer intento con un guion largo (—) en el JSON del `curl` devolvió 500 (`DecoderFallbackException: Unable to translate bytes... from specified code page`) — el shell de Windows/Git Bash mandó el carácter como una secuencia de bytes inválida para UTF-8 antes de salir del cliente; el backend rechazó correctamente el JSON malformado. Confirmado no es un bug de la app reintentando el mismo request con texto ASCII puro (200 OK). No se documenta como hallazgo de contrato en `design.md` por no ser causa raíz de la aplicación.
- [x] 4.5 Eliminación (soft-delete) y restauración (`DELETE /:id`, `PATCH /:id/restore`) — confirmado sobre dos NCs de prueba distintas: `deletedAt` se setea/limpia correctamente, audit trail `ELIMINADA`/`RESTAURADA`, y el filtro `showDeleted` refleja el estado esperado en cada paso (ver 4.2).
- [x] 4.6 Crear, actualizar y cerrar una Acción Correctiva — confirmado en secuencia: `POST /acciones-correctivas` → `estado: 'PENDIENTE'`; `PATCH /acciones-correctivas/:acId {estado: 'EN_EJECUCION'}` → `estado: 'EN_EJECUCION'`; `POST /acciones-correctivas/:acId/cerrar` con `descripcionEvidencia`/`evidenciaUrl` → `estado: 'CERRADA'`, `descripcionEvidencia` y `fechaCierre` persistidos.

## 5. Verificar vinculaciones (QE y Documentos)

- [x] 5.1 Crear un Quality Event con `origen: 'O2_NC_DETECTADA'`/`ncId` (`QE-2026-007`, 201) referenciando la No Conformidad, luego `PATCH /:id { qeGeneradoId }` — confirmado: `qeGeneradoId` persistido en la NC, `ncId` persistido en el QE, ambos apuntando entre sí contra el backend real.
- [x] 5.2 Vincular y desvincular un Documento (`POST`/`DELETE /:ncId/documentos-vinculados[/:documentoId]`) — confirmado: `documentosVinculados` pasa de `[]` a `[{id, codigo, titulo, estado}]` tras vincular y vuelve a `[]` tras desvincular, con audit trail `NC_VINCULADO`/`NC_DESVINCULADO`.

## 6. Verificar las transiciones de estado principales por API directa

- [x] 6.1 Ejecutado el ciclo completo `ABIERTA → EN_INVESTIGACION → ANALISIS_COMPLETADO → EN_EJECUCION → PENDIENTE_CIERRE → CERRADA` vía `PATCH /:id { estado }` directo — confirmado: 200 en los 5 pasos, audit trail con una entrada `CAMPO_EDITADO`/`campoModificado: 'estado'` por transición (`valorAnterior`/`valorNuevo` correctos en cada una). Notificación best-effort: código confirmado por lectura (`ActualizarNoConformidadHandler.cs` líneas 180-199, try/catch que nunca bloquea la respuesta) pero no observable en este escenario concreto — el único destinatario candidato (`reportadoPorId`) era el propio actor (mismo usuario creó la NC y ejecutó las transiciones) y la única AC de la NC ya estaba `CERRADA` (excluida por el filtro), así que la lista de destinatarios calculada era vacía por diseño, no por fallo.
- [x] 6.2 Confirmado el bloqueo 409 (`nonconformities:errors.editBlockedByStatus`) al intentar `PATCH /:id { estado }` sobre una NC en `CERRADA` y, por separado, sobre una en `ANULADA`.
- [x] 6.3 **Hallazgo real, no bloqueante**: el backend NO valida el orden de la secuencia de estados en `PATCH /:id` — una NC en `ABIERTA` aceptó `estado: 'PENDIENTE_CIERRE'` directamente (200), saltando los tres estados intermedios. Confirmado que esto coincide exactamente con `be-no-conformidades-api` spec.md, Requirement "Edición parcial bloqueada en estados terminales": el único requisito documentado es el 409 en `CERRADA`/`ANULADA` — nunca se especificó una máquina de estados server-side que valide el orden de transición (a diferencia de QE, cuya máquina de estados sí es "estricta" por `CLAUDE.md`). No es un mismatch de contrato de este change — es el comportamiento ya documentado en la spec de M2, y coincide con que tampoco hay ninguna UI que intente ofrecer solo las transiciones válidas (ver hallazgo principal del proposal). Documentado también en `design.md`.

## 7. Tests y cierre del ciclo

- [x] 7.1 Inspeccionados los tests de No Conformidades — dos archivos frontend ejercitan `msw/node`/`setupServer` real: `src/mocks/handlers/nonconformities.handlers.test.ts` (testea explícitamente los HANDLERS MSW mismos: `setupServer(...nonconformityHandlers)`) y `src/router/nonconformitiesAccess.test.tsx` (testea el `RoleGuard`/routing de `/nonconformities` usando MSW como doble de red offline — no depende de si existe backend real, migrarlo requeriría levantar Postgres/backend real dentro de la suite de tests unitarios, fuera de alcance de este change). Ninguno de los dos se migra — mismo criterio y misma conclusión que `cutover-incidentes` 10.1. Los tests backend (`ShcMvpEndPoint.Tests/Features/NoConformidades/*`) ya corren contra un `ShacDbContext` de test real, sin relación con MSW.
- [x] 7.2 Suite completa corrida sin regresiones: backend 451/451, frontend 1310 passed + 1 expected fail (mismo patrón pre-existente ya documentado en `cutover-auth`/`cutover-incidentes`).
- [x] 7.3 Hallazgo real documentado en `design.md` ("Hallazgos de verificación"): el backend no valida el orden de la secuencia de estados de la NC en `PATCH /:id` — comportamiento ya coincidente con `be-no-conformidades-api` spec.md (solo exige bloqueo 409 en estados terminales), no un mismatch introducido por este change. Sin otros hallazgos bloqueantes — el 500 encontrado en el intento de anulación (tasks.md 4.4) se diagnosticó como un artefacto de codificación del shell de verificación, no un bug de la aplicación, y no se documenta como hallazgo de `design.md`.
- [x] 7.4 Revertido `shc-controldoc/.env.development` a `VITE_API_BASE_URL=`, `VITE_ENABLE_MSW=true` (estado original).
- [x] 7.5 Confirmado: `shc-controldoc/.env.production` no requiere cambios — ya tenía `VITE_ENABLE_MSW=false` desde `cutover-auth`, mismo Open Question de hosting pendiente.
- [x] 7.6 Evaluado — no aplica, mismo criterio que cutovers anteriores: el estado global de No Conformidades en la tabla "Módulos del Sistema" de `CLAUDE.md` sigue siendo "Implementado (mock)" a nivel de toda la app hasta que los módulos restantes (Quality Events, Dashboard, Users) tengan su propio cutover.
