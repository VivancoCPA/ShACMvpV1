## 1. `qe-2026-004` — anchor "en plazo" case for KPI-01/02, EFECTIVO case for KPI-05

- [x] 1.1 In `src/mocks/fixtures/quality-events.fixtures.ts`, on `qe-2026-004`, shift these top-level fields: `fechaHoraEvento` → `'2026-06-08T16:00:00Z'`, `fechaHoraReporte` → `'2026-06-09T09:00:00Z'`, `reporteExternoRef.fechaRecepcion` → `'2026-06-09'`, `causaRaizFirmadaEn` → `'2026-06-22T10:00:00Z'`, `fechaCierre` → `'2026-07-03T10:00:00Z'`, `fechaVerificacionProgramada` → `'2026-07-20T00:00:00Z'`, `fechaVerificacionRealizada` → `'2026-07-18T10:00:00Z'`, `creadoEn` → `'2026-06-09T09:05:00Z'`, `actualizadoEn` → `'2026-07-18T10:00:00Z'`. Leave `resultadoVerificacion: 'EFECTIVO'`, `severidad: 'BAJA'`, `estado: 'VERIFICADO'`, `ciclo: 1` unchanged.
- [x] 1.2 On the same fixture, shift `auditTrail` timestamps to match the new timeline: `aud-qe-004-1` (CREADO) → `'2026-06-09T09:05:00Z'`, `aud-qe-004-2` (→EN_INVESTIGACION) → `'2026-06-10T08:00:00Z'`, `aud-qe-004-3` (→ANALISIS_COMPLETADO) → `'2026-06-22T09:00:00Z'`, `aud-qe-004-4` (→EN_EJECUCION) → `'2026-06-24T08:00:00Z'`, `aud-qe-004-5` (→PENDIENTE_CIERRE) → `'2026-07-01T10:00:00Z'`, `aud-qe-004-6` (→CERRADO) → `'2026-07-03T10:00:00Z'`, `aud-qe-004-7` (→VERIFICADO) → `'2026-07-18T10:00:00Z'`. Do not change `estadoAnterior`/`estadoNuevo`/`accion`/`realizadoPorId` on any entry.
- [x] 1.3 In the `qeAccionesCorrectivas` map at the top of the same file, add a new `'qe-2026-004'` entry (currently absent) with one `AccionCorrectivaQE` of `estado: 'CERRADA'`, `plazoFecha: '2026-06-30'`, `fechaCierre: '2026-06-28T10:00:00Z'`, `creadoEn: '2026-06-24T08:10:00Z'`, `actualizadoEn: '2026-06-28T10:00:00Z'` (before the current QE's `fechaVerificacionRealizada`), following the shape/fields of the existing `'qe-2026-002'` entry (id `ac-qe-004-1`, `qeId: 'qe-2026-004'`, descriptive `titulo`/`descripcion`/`responsableId`/`responsableNombre`/`prioridad`/`descripcionEvidencia`).

## 2. `qe-2026-009` — "fuera de plazo" case for KPI-01/02, data point for KPI-07

- [x] 2.1 On `qe-2026-009`, shift `fechaHoraEvento` → `'2026-04-01T22:00:00Z'`, `fechaHoraReporte` → `'2026-04-02T06:00:00Z'`, `causaRaizFirmadaEn` → `'2026-07-06T10:00:00Z'`, `fechaCierre` → `'2026-07-18T10:00:00Z'`, `creadoEn` → `'2026-04-02T06:05:00Z'`, `actualizadoEn` → `'2026-07-18T10:00:00Z'`. Leave `severidad: 'BAJA'`, `estado: 'CERRADO'`, `ciclo: 1` unchanged.
- [x] 2.2 On the same fixture's `auditTrail`: update `aud-qe-009-1` (CREADO) timestamp → `'2026-04-02T06:05:00Z'`; insert a new entry `aud-qe-009-2` (`accion: 'ESTADO_CAMBIADO'`, `estadoAnterior: 'EN_INVESTIGACION'`, `estadoNuevo: 'ANALISIS_COMPLETADO'`, `realizadoPorId: 'user-003'`, `realizadoPorNombre: 'María Castro'`, `timestamp: '2026-07-05T09:00:00Z'`, `generadoPorIA: false`) directly after it; renumber and update timestamps of the remaining entries: the existing `CAUSA_RAIZ_APROBADA` entry → id `aud-qe-009-3`, `timestamp: '2026-07-06T10:00:00Z'`; the existing `→PENDIENTE_CIERRE` entry → id `aud-qe-009-4`, `timestamp: '2026-07-10T10:00:00Z'`; the existing `→CERRADO` entry → id `aud-qe-009-5`, `timestamp: '2026-07-18T10:00:00Z'`.

## 3. `qe-2026-015` — reincidence (ciclo>1) case for KPI-03, second "fuera de plazo" case for KPI-01/02

- [x] 3.1 On `qe-2026-015`, change `estado` from `'PENDIENTE_CIERRE'` to `'CERRADO'`; add `fechaCierre: '2026-07-08T10:00:00Z'` and `cierreFirmaSupervisorId: 'user-003'` (RN-QE-004 dual signature — `cerradoPorId: 'user-004'` is already present); update `actualizadoEn` → `'2026-07-08T10:00:00Z'`. Leave `ciclo: 2` and `severidad: 'ALTA'` unchanged.
- [x] 3.2 Add a new `auditTrail` entry `aud-qe-015-5` (`accion: 'ESTADO_CAMBIADO'`, `estadoAnterior: 'PENDIENTE_CIERRE'`, `estadoNuevo: 'CERRADO'`, `realizadoPorId: 'user-004'`, `realizadoPorNombre: 'Ana Torres'`, `timestamp: '2026-07-08T10:00:00Z'`, `generadoPorIA: false`) after the existing 4 entries.

## 4. `qe-2026-017` — NO_EFECTIVO case for KPI-05 (no estado change)

- [x] 4.1 On `qe-2026-017`, add a new top-level field `fechaVerificacionRealizada: '2026-07-12T10:00:00Z'`. Leave `estado: 'REABIERTO'`, `ciclo: 2`, and `resultadoVerificacion: 'NO_EFECTIVO'` unchanged.
- [x] 4.2 In the `qeAccionesCorrectivas` map, add a new `'qe-2026-017'` entry with one `AccionCorrectivaQE` of `estado: 'CERRADA'`, `plazoFecha: '2026-01-25'`, `fechaCierre: '2026-01-22T10:00:00Z'`, `creadoEn: '2026-01-08T22:10:00Z'`, `actualizadoEn: '2026-01-22T10:00:00Z'` (id `ac-qe-017-1`, `qeId: 'qe-2026-017'`), with `descripcionEvidencia` noting the fix was applied but proved insufficient (consistent with the fixture's existing `resultadoVerificacion: 'NO_EFECTIVO'` narrative).

## 5. Verification

- [x] 5.1 Run the existing fixture/handler test suites (`quality-events.fixtures`, `dashboard.handlers.test.ts`, and any test referencing `qe-2026-004`, `qe-2026-009`, `qe-2026-015`, or `qe-2026-017` by id/date) and fix any assertion that depended on the old dates, `estado`, or audit-trail shape of these four fixtures.
- [x] 5.2 Run `npm run typecheck` and `npm run lint` in `shc-controldoc/`.
- [x] 5.3 Start the app with `VITE_ENABLE_MSW=true`, log in as `jefe.calidad@shac.pe`, open `/dashboard`, and confirm in the browser: KPI-01 and KPI-03 render a percentage strictly between 0% and 100%; KPI-02 and KPI-07 render a positive, non-zero número de días; KPI-05 renders a percentage strictly between 0% and 100%; KPI-04/06/08/09 still render as before (unaffected).
- [x] 5.4 Confirm no other dashboard role view (`OperarioDashboard`, `SupervisorDashboard`) regresses by spot-checking `/dashboard` as an `OPERARIO` and a `SUPERVISOR` user — these four fixtures may feed `qePorEstado`/semáforo widgets there too.
