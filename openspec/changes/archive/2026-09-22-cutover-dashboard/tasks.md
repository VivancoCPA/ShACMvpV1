## 1. Preparar el entorno local

- [x] 1.1 Confirmar `docker-compose up -d` en `ShcMvpEndPoint/` con `shac-postgres` sano — confirmado, `shac-postgres` ya estaba `Up 11 days (healthy)` en `0.0.0.0:5435->5432/tcp`, de una sesión anterior.
- [x] 1.2 Confirmar `dotnet run` levanta sin errores contra Postgres dev, anotar el puerto real — confirmado, backend corriendo en `http://localhost:5072` (responde 405 en `GET /api/auth/login`, esperado ya que el endpoint es POST-only; logs muestran queries EF Core reales ejecutándose contra Postgres).
- [x] 1.3 Ninguno de los usuarios QA reutilizables existentes cubría los 6 roles con acceso a Dashboard en una misma empresa. Se creó una empresa QA dedicada ("Cutover Dashboard QA S.A.C.", `POST /api/empresas` como `SUPERADMIN`) y se asignó `multi.empresa.qa@shac.dev` (password ya conocida, `QaTest_2026!`) como `ADMINISTRADOR_EMPRESA` vía `AsignarUsuarioEmpresa` — no se tocó ninguna asignación existente de ese usuario en otras empresas, solo se sumó una nueva. Con esa sesión `ADMINISTRADOR_EMPRESA` se crearon 6 usuarios nuevos (uno por rol con acceso a Dashboard), cada uno con contraseña temporal real devuelta por `POST /api/users` y registrada abajo; para `SUPERVISOR` se creó primero un área (`POST /api/areas` como el `JEFE_CALIDAD_SYST` recién creado, `area-4d812017e5a9`) porque el schema lo exige en la creación:\n  - `dash.jefecalidad.qa@shac.dev` / `o5PzKS3H` — `JEFE_CALIDAD_SYST`\n  - `dash.operario.qa@shac.dev` / `l5gvE4AB` — `OPERARIO`\n  - `dash.supervisor.qa@shac.dev` / `sTZEE63J` — `SUPERVISOR` (área `area-4d812017e5a9`)\n  - `dash.jefecontroldoc.qa@shac.dev` / `Bn8Odszv` — `JEFE_CONTROL_DOCUMENTARIO`\n  - `dash.auditor.qa@shac.dev` / `TfMNpz8a` — `AUDITOR_INTERNO`\n  - `dash.altadireccion.qa@shac.dev` / `QnhkDmh2` — `ALTA_DIRECCION`\n  Para el rol sin acceso se reutiliza `multi.empresa.qa@shac.dev` mismo (`ADMINISTRADOR_EMPRESA` en esta empresa) y `superadmin@shac.dev` (`SUPERADMIN`), ambos con password ya conocida. Empresa nueva y sin datos operativos previos (QE/Incidentes/Documentos/NC) — los widgets se verifican con dataset vacío pero con forma de respuesta real (ver nota de metodología en 3.3).

## 2. Confirmar que no hay mismatch de contrato (re-verificación previa a tocar el entorno)

- [x] 2.1 Releído `ObtenerKpisEndpoint.cs`/`CargarHorasTrabajadasEndpoint.cs`/`CargarKpi04AnioAnteriorEndpoint.cs` — confirmado, `git diff --stat` sobre `ShcMvpEndPoint/Features/Dashboard`, `Extensions/EndpointExtensions.cs`, `shc-controldoc/src/api/endpoints/dashboard.api.ts` y `shc-controldoc/src/features/dashboard` desde la investigación previa a este proposal da vacío (sin cambios locales).
- [x] 2.2 Releído `Extensions/EndpointExtensions.cs` — confirmado, las 4 rutas de Dashboard (`ObtenerSummary`, `ObtenerKpis`, `CargarHorasTrabajadas`, `CargarKpi04AnioAnterior`) siguen registradas sin cambios, mismos gates de rol documentados en `design.md` (`RolesConAccesoDashboard` para KPIs, `SUPERADMIN` para los dos endpoints de carga manual).
- [x] 2.3 Sin discrepancias detectadas — no aplica documentar nada nuevo en `design.md`.

## 3. Apuntar el frontend al backend real

- [x] 3.1 Editado `shc-controldoc/.env.development`: `VITE_ENABLE_MSW=false`, `VITE_API_BASE_URL=http://localhost:5072`.
- [x] 3.2 Login confirmado contra el backend real con `dash.jefecalidad.qa@shac.dev` (uno de los usuarios QA de 1.3) — `accessToken` real devuelto por `POST /api/auth/login`.
- [x] 3.3 **Nota de metodología**: sin herramienta de automatización de navegador disponible en este entorno (sin Playwright/similar) — se verifica el contrato completo por API directa (`curl`, JWT real por login) en vez de clic en la UI, mismo criterio que `cutover-usuarios`/`cutover-no-conformidades`. El contrato de tipos frontend↔backend ya se confirmó por lectura de código en `design.md`; esta verificación confirma el comportamiento observable real contra Postgres, no el renderizado pixel a pixel de cada widget.

## 4. Verificar el resumen de Dashboard para los 6 roles con acceso

- [x] 4.1 `GET /api/dashboard/summary` como `OPERARIO` (`dash.operario.qa`) — confirmado: `{"rol":"OPERARIO","data":{"misIncidentesReportados":[],"misQEReportados":[],"accionesCorrectivasAsignadas":[],"documentosPendientesLectura":[]}}`, coincide campo a campo con `OperarioDashboardData`, sin ningún campo `undefined` (arrays vacíos porque la empresa QA es nueva, sin datos operativos previos — ver nota de metodología 3.3).
- [x] 4.2 `GET /api/dashboard/summary` como `SUPERVISOR` (`dash.supervisor.qa`) — confirmado: `rol:"SUPERVISOR"`, incluye `kpisArea` (5 KPIs reales calculados), `qePorEstado`, `qeAbiertosPorTipo` con las 4 claves de `QEType`, `accionesCorrectivasVencidas: []` (array presente y correctamente tipado — sin datos de AC en este dataset nuevo no se puede ejercitar la distinción `PENDIENTE`/`EN_EJECUCION` en sí, pero el campo existe con la forma correcta, que es lo que este change verifica: contrato de red, no la lógica de agregación ya validada en agosto).
- [x] 4.3 `GET /api/dashboard/summary` como `JEFE_CALIDAD_SYST` (`dash.jefecalidad.qa`) — confirmado: `rol:"JEFE_CALIDAD"`, incluye `kpis` (9 KPIs, KPI-01 a KPI-09), `qePorEstado` con las 8 claves de `QEStatus`, `tendenciaMensualVolumen` (12 meses), `tendenciaMensualKpis` con las 3 claves (`KPI-01`/`KPI-04`/`KPI-05`) — coincide campo a campo con `JefeCalidadDashboardData`.
- [x] 4.4 `GET /api/dashboard/summary` como `JEFE_CONTROL_DOCUMENTARIO` (`dash.jefecontroldoc.qa`) — confirmado: `{"rol":"JEFE_CONTROL_DOC","data":{}}`, coincide con `JefeControlDocDashboardData = Record<string, never>` (objeto vacío, no `null`/`undefined`).
- [x] 4.5 `GET /api/dashboard/summary` como `AUDITOR_INTERNO` (`dash.auditor.qa`) — confirmado: `rol:"AUDITOR"`, incluye `hallazgosPorNorma`, `hallazgosPorEstado` con las 8 claves de `QEStatus`, `evidenciasHallazgos`, `tasaCierreEnPlazoPorArea` — coincide con `AuditorDashboardData`.
- [x] 4.6 `GET /api/dashboard/summary` como `ALTA_DIRECCION` (`dash.altadireccion.qa`) — confirmado: `rol:"ALTA_DIRECCION"`, incluye `kpisEstrategicos` (9 KPIs), `resumenPorModulo` con las 4 claves de módulo, `tendenciaTrimestral` (4 trimestres), `comparativaMensual` con las 3 claves de KPI, `reaperturas`, `acsConSolicitudAjustePlazo` — coincide campo a campo con `AltaDireccionDashboardData`.

**Nota general de las secciones 4.1-4.6**: no se verificó el renderizado pixel a pixel de cada uno de los 25 componentes de widgets (sin herramienta de navegador, ver 3.3) — se verificó que el payload real que cada widget consumiría tiene exactamente la forma que sus tipos TypeScript esperan, sin ningún campo `undefined`/faltante, que es el riesgo real identificado en `design.md` (contrato de red, no renderizado).

## 5. Verificar KPIs por periodo

- [x] 5.1 `GET /api/dashboard/kpis?periodo=2026-09` como `JEFE_CALIDAD_SYST` — confirmado: devuelve un array de 9 `KpiResult` (KPI-01 a KPI-09), cada uno con `kpiId`/`valor`/`meta`/`metaTipo`/`semaforo`/`periodo`/`calculadoEn`/`valorPeriodoAnterior`/`distribucion`, coincidente en forma exacta con `KpiResult` (frontend).
- [x] 5.2 Confirmados los valores de `meta`/`metaTipo` de los 9 KPIs contra `kpi.constants.ts`: KPI-01 `meta:90`, KPI-02 `meta:15`, KPI-03 `meta:5`, KPI-04 `meta:10` (`REDUCCION_INTERANUAL`), KPI-05 `meta:85`, KPI-06 `meta:100`, KPI-07 `meta:7`, KPI-08 `meta:3`, KPI-09 `meta:3` — coinciden dígito por dígito con el backend real (no solo con el valor estático ya comparado en agosto). `valor:0` en todos por ser una empresa QA nueva sin datos operativos — confirma que `DashboardKpiConstants.cs` aplica la meta correcta incluso cuando el numerador es 0, no solo que el cálculo interno funciona con datos poblados (ya validado en la verificación de agosto).

## 6. Verificar el acceso restringido a Dashboard

- [x] 6.1 Confirmado por API directa: `GET /api/dashboard/kpis` responde 403 para `ADMINISTRADOR_EMPRESA` (`multi.empresa.qa` en "Cutover Dashboard QA S.A.C."), `ADMINISTRADOR_SISTEMA` (`dash.adminsistema.qa`, creado para esta verificación) y `SUPERADMIN`.
- [x] 6.2 Confirmado por API directa que `GET /api/dashboard/summary` **no** se comporta igual para los 3 roles: `ADMINISTRADOR_EMPRESA` y `ADMINISTRADOR_SISTEMA` responden 403 `"Rol sin acceso al dashboard."` (coincide con lo predicho), pero `SUPERADMIN` responde **401** `"No hay una empresa activa en la sesión."` — no 403. Causa raíz confirmada y documentada en `design.md` D3: `SUPERADMIN` nunca tiene `empresaActivaId`, así que el `RequireEmpresaActivaFilter` compartido de toda la app corta antes de llegar al chequeo de rol del handler. No es un bug — mismo criterio de "diagnóstico antes de fix", documentado sin corregir código.

## 7. Verificar al menos una exportación

- [x] 7.1 Sin herramienta de navegador disponible (3.3), se verificó por lectura de código (`ExportButton.tsx`) que `handleSelect` arma `sections` con `buildJefeCalidadExportSections`/`buildAltaDireccionExportSections` (funciones puras) y llama a `exportToExcel`/`exportToPdf` (generación 100% client-side, sin `fetch`/`axios` propio) — confirmado que no hace ninguna llamada de red en el momento del clic.
- [x] 7.2 **Hallazgo que refina el proposal**: `ExportButton.tsx` no solo consume `useDashboardSummary()` (`dashboard.api.ts`) — también consume `useAccionesRequeridas()` (que internamente llama a `useQualityEvents`/`useNonconformities`/`useIncidents`/`useDocuments`, cada uno contra su propio endpoint real) y `useHeatmapPorLocal()` (`useLocales`/`useIncidentList`). Estas llamadas ya ocurren al cargar el dashboard (no las dispara el clic de exportar), y los 4 módulos que alimentan ya fueron cutover-eados independientemente (`cutover-quality-events`, `cutover-no-conformidades`, `cutover-incidentes`, `cutover-documentos`, `cutover-catalogos` para Locales) — se confirmó por API directa que sus 5 endpoints (`GET /api/quality-events`, `/api/nonconformities`, `/api/incidents`, `/api/documents`, `/api/locales`) responden 200 contra el backend real como `JEFE_CALIDAD_SYST` en la empresa QA de este change. No es un mismatch de contrato — es la confirmación de que el pipeline completo de datos que alimenta el export funciona de punta a punta, más allá del propio `dashboard.api.ts`.

## 8. Verificar los endpoints `SUPERADMIN`-only sin consumidor en la UI

- [x] 8.1 Login como `SUPERADMIN` y `PUT /api/empresas/:empresaId/dashboard/horas-trabajadas` con `{areaId, periodo:"2026-09", horas:1600}` sobre la empresa QA de este change — confirmado, 200, persistido correctamente (`actualizadoEn` real).
- [x] 8.2 Login como `SUPERADMIN` y `PUT .../dashboard/kpi04-anio-anterior` con `{periodo:"2025-09", valor:8.5}` — confirmado, 200, persistido correctamente.
- [x] 8.3 Confirmado por lectura de `shc-controldoc/src/features/dashboard/api/dashboard.api.ts`: solo exporta `getDashboardKpis`/`getDashboardSummary`, sin ninguna función para estos dos endpoints — sigue sin haber consumidor en el frontend. Documentado como hallazgo informativo en `proposal.md`, sin corregir.

## 9. Tests y cierre del ciclo

- [x] 9.1 Inspeccionados los 6 archivos frontend de Dashboard que usan `setupServer`/`msw/node`: `mocks/handlers/dashboard.handlers.test.ts` (testea los HANDLERS MSW mismos, `setupServer(...dashboardHandlers)` — sin equivalente posible contra backend real) y `features/dashboard/hooks/useDashboardSummary.test.ts`, `useDashboardKpis.test.ts`, `components/HeatmapIncidentesWidget.test.tsx`, `pages/AltaDireccionDashboard.test.tsx`, `pages/JefeCalidadDashboard.test.tsx` (usan MSW como doble de red offline para ejercitar hooks/componentes reales — migrarlos requeriría levantar Postgres/backend real dentro de la suite de tests unitarios, fuera de alcance de este change). Ninguno de los 6 se migra — mismo criterio y misma conclusión que `cutover-usuarios` 9.1/`cutover-no-conformidades` 7.1/`cutover-incidentes` 10.1.
- [x] 9.2 Suite completa corrida sin regresiones: backend `dotnet test` → 451/451 passed; frontend `npx vitest run` → 1310 passed + 1 expected fail (mismo patrón pre-existente ya documentado en `cutover-auth`/`cutover-incidentes`/`cutover-no-conformidades`/`cutover-usuarios`).
- [x] 9.3 Documentados dos hallazgos reales en `design.md`/`tasks.md`, ninguno bloqueante ni corregido: (1) D3 — `GET /api/dashboard/summary` responde 401 (no 403) para `SUPERADMIN`, causa raíz confirmada (`RequireEmpresaActivaFilter` corre antes que el chequeo de rol del handler, y `SUPERADMIN` nunca tiene `empresaActivaId`), comportamiento correcto y compartido por toda la app, no específico de Dashboard; (2) sección 7.2 — el export de Alta Dirección/Jefe de Calidad también depende de los endpoints ya cutover-eados de QE/NC/Incidentes/Documentos/Locales (vía `useAccionesRequeridas`/`useHeatmapPorLocal`), confirmados 200 contra el backend real.
- [x] 9.4 Revertido `shc-controldoc/.env.development` a `VITE_API_BASE_URL=`, `VITE_ENABLE_MSW=true` (estado original) — confirmado con `git status --short` vacío sobre el archivo.
- [x] 9.5 Confirmado: `shc-controldoc/.env.production` no requiere cambios — ya tenía `VITE_ENABLE_MSW=false` desde `cutover-auth`, mismo Open Question de hosting pendiente; `git status --short` vacío confirma que no se tocó.
- [x] 9.6 Evaluado — no aplica cambiar la tabla "Módulos del Sistema" de `CLAUDE.md`, mismo criterio que `cutover-usuarios` 9.6: el estado por módulo ("Implementado (mock)") describe el modo de desarrollo diario (MSW activo salvo ventana de verificación), no si su contrato de red ya fue cutover-eado. Con Dashboard cerrado, los ocho módulos de dominio del roadmap original quedan cutover-eados — pero el Open Question de hosting/dominio de producción (heredado de `cutover-auth`, aún sin resolver) sigue siendo lo único que determinaría un cambio real de estado a nivel de toda la app.
