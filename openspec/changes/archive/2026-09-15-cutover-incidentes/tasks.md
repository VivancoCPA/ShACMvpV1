## 1. Preparar el entorno local

- [x] 1.1 Confirmar `docker-compose up -d` en `ShcMvpEndPoint/` con `shac-postgres` sano — confirmado, `shac-postgres` estaba `Up 4 days (healthy)`.
- [x] 1.2 Confirmar `dotnet run` levanta sin errores contra Postgres dev, anotar el puerto real — confirmado, `http://localhost:5072` (perfil `http`), responde 401 en `/api/incidents` sin token. Nota: el arranque tardó ~70s porque `DevSeedAreasExtensions` re-siembra 19 áreas por cada empresa existente y la BD dev acumuló 8488 filas en `empresas` (cruft de sesiones de verificación anteriores) — no bloqueante para este change, se documenta como hallazgo en design.md.
- [x] 1.3 Confirmar usuarios QA multi-empresa reutilizables de `cutover-auth`/`cutover-catalogos` siguen sirviendo para probar la excepción de `empresaId` offline (sección 4) — confirmado, los 5 fixtures QA de `cutover-auth` siguen existiendo. Drift ya documentado se mantiene: `mono.empresa.qa@shac.dev` tiene 3 membresías `ACTIVO` (es el multi-empresa real), `multi.empresa.qa@shac.dev` tiene 1 (es el mono-empresa real) — se usará `mono.empresa.qa@shac.dev` para la verificación de la sección 9.5.

## 2. Backend — extender el modelo de datos de Incidente (D1)

- [x] 2.1 Agregar a `Domain/Entities/Incidente.cs`: `Testigos`, `PersonalInvolucrado`, `EquiposInvolucrados` (`string[]?`), `AtencionMedicaRequerida` (`bool?`), `AtencionMedicaDescripcion` (`string?`), `NotificacionAmbientalRequerida` (`bool?`), `GeoLat`/`GeoLng`/`GeoCapturadoEn` (`geoUbicacion`), y la colección `Evidencias` (nueva entidad `IncidenteEvidencia`, FK a `IncidenteId`, mismo patrón que `IncidenteAuditTrail`).
- [x] 2.2 Agregar a `CrearIncidenteCommand`: `LocalId?`, `ZonaId?`, `UbicacionX?`, `UbicacionY?`, `GeoUbicacion?` (`{ Lat, Lng, CapturadoEn }`), `Evidencias?` (lista de `{ Url, Nombre, Tipo, TamanioKb, Descripcion? }`).
- [x] 2.3 Agregar a `ActualizarInvestigacionCommand`: `Testigos?`, `PersonalInvolucrado?`, `EquiposInvolucrados?`, `AtencionMedicaRequerida?`, `AtencionMedicaDescripcion?`, `NotificacionAmbientalRequerida?`.
- [x] 2.4 Actualizar `CrearIncidenteHandler`/`ActualizarInvestigacionHandler` para persistir los campos nuevos de cada command. Hallazgo adicional corregido en el mismo cambio: `ObtenerIncidenteHandler`/`ListarIncidentesHandler` (Dapper) no proyectaban `qe_id` pese a existir en la entidad desde `be-quality-events` D9 — agregado junto con los campos nuevos ya que se estaba tocando el mismo SQL.
- [x] 2.5 Actualizar `CrearIncidenteValidator` — agregado límite de 5 evidencias (mismo tope que `mobileIncidentReportSchema`). `ActualizarInvestigacionCommand` no tiene validator hoy (confirmado, no existe `ActualizarInvestigacionValidator` pese a que un comentario viejo lo mencionaba) — no se introduce uno nuevo, consistente con que ningún otro campo opcional de ese command se valida server-side hoy.
- [x] 2.6 Migración EF Core `AddIncidentesInvestigacionExtendida`: 9 columnas nuevas en `incidentes` + tabla `incidente_evidencias` (FK cascade a `incidentes`). Generada y aplicada contra Postgres dev sin errores.
- [x] 2.7 (hallazgo durante implementación, ver design.md D1 revisada) `ActualizarInvestigacionCommand` también agrega `Evidencias?` — `IncidentForm.tsx` (escritorio) permite adjuntar evidencia nueva tanto al crear como al editar (mismo componente `EvidenciasZona` en ambos modos), no solo en la creación mobile como asumía la D1 original. `IncidenteEvidenciaInput` se movió a `Features/Incidentes/Shared/` para compartirlo entre ambos commands. `ActualizarInvestigacionHandler` agrega (nunca reemplaza) vía `db.IncidenteEvidencias.Add(...)`, mismo patrón ya usado para el audit trail (incidente tracked desde la query, `collection.Add()` con Guid client-generado se trataría como `Modified`, no `Added`). Sin migración nueva (misma tabla `incidente_evidencias`).

## 3. Backend — upload real de evidencias (D2)

- [x] 3.1 Crear `IncidenteEvidenciaStorageService` (`Features/Incidentes/Shared/`), calcado de `PlanoStorageService.cs`: guarda el `IFormFile` bajo `wwwroot/uploads/incidentes/{empresaId}/`, devuelve la URL estática.
- [x] 3.2 Crear endpoint `POST /api/incidents/evidencias` (`Features/Incidentes/SubirEvidencia/`): requiere sesión con empresa activa, valida tipo y tamaño (máx. 10 MB) vía `IncidenteEvidenciaValidator`, devuelve `{ url, nombre, tamanioKb }`. Campo del form-data: `evidencia`. Tipos aceptados ampliados a JPEG/PNG/WEBP/PDF (unión de `mobileIncidentReportSchema` y `incidentForm.schema.ts` — el formulario de escritorio también acepta PDF, hallazgo del mismo pase que 2.7).
- [x] 3.3 Registrado en `EndpointExtensions.MapFeatureEndpoints()`/`AddFeatureHandlers()` (`Program.cs` llama a `app.MapFeatureEndpoints()`, patrón manual de VSA de este proyecto — no hay auto-discovery de endpoints).

## 4. Backend — excepción de `empresaId` en creación offline (D4)

- [x] 4.1 Agregar `EmpresaId?` a `CrearIncidenteCommand`.
- [x] 4.2 En `CrearIncidenteEndpoint`: si `EmpresaId` viene presente, valida membresía vía `SessionResolver.GetRolEfectivoAsync(actorId, empresaId, ct)` (reusa el mismo mecanismo que Auth/Empresas, sin necesidad de código nuevo de resolución) antes de usarla; `Results.Forbid()` (403) si no es miembro. Ausente, comportamiento sin cambios (`GetEmpresaActivaId()`). La validación quedó en el Endpoint, no en el Handler, porque `SessionResolver` ya resuelve `empresaId` antes de invocar `CrearIncidenteHandler.HandleAsync` (que sigue recibiendo un `Guid empresaId` ya resuelto, sin cambios en su firma).
- [x] 4.3 Comentario explícito agregado en `CrearIncidenteCommand.cs` y `CrearIncidenteEndpoint.cs` marcando esto como la única excepción de multi-tenancy basada en body de todo el backend, y por qué (ver design.md D4).

## 5. Backend — cierre de Acción Correctiva (D5)

- [x] 5.1 Crear endpoint `PATCH /api/incidents/{incidenteId}/acciones/{acId}/cerrar` (`Features/Incidentes/CerrarAccionCorrectiva/`) que arma un `ActualizarAccionCorrectivaCommand(Estado: CERRADA, DescripcionEvidencia, EvidenciaUrl)` y delega a `ActualizarAccionCorrectivaHandler` existente. Requirió alias (`IncCerrarAccionCorrectivaEndpoint`) en `EndpointExtensions.cs` — mismo choque de nombre que `CrearAccionCorrectiva`/`ActualizarAccionCorrectiva` entre `Features.NoConformidades` y `Features.Incidentes` ya documentado en ese archivo.
- [x] 5.2 Confirmado: `CerrarAccionCorrectivaCommand(string DescripcionEvidencia, string? EvidenciaUrl)` coincide exactamente con `cerrarAC.schema.ts` (`descripcionEvidencia` requerido vía `CerrarAccionCorrectivaValidator`, `evidenciaUrl` opcional) — cero cambios de frontend.

## 6. Frontend — subir evidencias reales en vez de `blob:` URL (D3)

- [x] 6.1 `IncidentQuickReportForm.tsx`: antes de llamar a `createIncident(payload)`, se sube cada foto de `photos` vía `subirEvidencia()` (`POST /api/incidents/evidencias`) y se reemplaza `url: p.previewUrl` por la URL real devuelta en el `evidencias` del payload final. Se restructuró `onSubmit` para construir `evidencias` solo en el camino online (el offline sigue encolando `photoBlobs` crudos, sin intentar upload sin red).
- [x] 6.2 El bloque de upload + `createIncident` comparte un único `try/catch`; cualquier fallo (upload o creación) pasa por `classifySubmitError` y cae a `queueLocally()` si es de red — sin código nuevo de clasificación.
- [x] 6.3 `useOfflineIncidentSync.ts` → `syncOne`: `buildEvidenciasFromBlobs` (generaba `blob:` URLs) reemplazado por `uploadEvidenciasFromBlobs` (async), que sube cada `Blob` vía `subirEvidencia()` antes de `createMutationRef.current.mutateAsync(...)` y arma `evidencias` con las URLs reales devueltas.
- [x] 6.4 Un upload fallido dentro de `uploadEvidenciasFromBlobs` propaga la excepción hacia el `try/catch` de `syncOne` (sin captura interna), que ya clasifica y aplica `MAX_SYNC_RETRIES`/`markError` — ninguna evidencia parcial llega a `createIncidentOfflineSync` porque el throw ocurre antes de construir el payload.
- [x] 6.5 Confirmado: `compressPhoto.ts` (corre antes de encolar, sin cambios), `useGeolocationCapture.ts` (sin cambios) y `offlineQueue` (`photoBlobs`/`photoCaptions` sin cambios de shape) no requirieron ningún ajuste — solo cambió qué URL termina en `evidencias[].url`.
- [x] 6.6 (hallazgo durante implementación, fuera del alcance original de la Sección 6 — ver design.md D1 revisada) `IncidentForm.tsx` (escritorio, create y edit) tenía el mismo defecto de `blob:` URL (`URL.createObjectURL(f)` en `mockEvidencias`). Corregido igual que 6.1/6.3: sube cada archivo real vía `subirEvidencia()` antes de armar el payload, tanto en creación como en edición (donde se agregan a `incident.evidencias ?? []` existentes). `updateIncidentInvestigacionSchema` (`createIncident.schema.ts`) se amplió con un campo `evidencias?` opcional para que el payload de `PATCH` lo acepte a nivel de tipos.

## 7. Apuntar el frontend al backend real

- [x] 7.1 Editado `shc-controldoc/.env.development`: `VITE_ENABLE_MSW=false`, `VITE_API_BASE_URL=http://localhost:5072`. Backend confirmado arriba (`dotnet run`, responde 401 en `/api/incidents` sin token).
- [x] 7.2 `npm run dev` sirve bien para las secciones 8 (CRUD/AC/QE) pero NO para el ciclo offline real: `vite dev` con `devOptions.enabled: true` de `vite-plugin-pwa` genera un `dev-sw.js` que falla con `ServiceWorker script evaluation failed` en Chromium headless (hallazgo de tooling, ver sección 9). Para la sección 9 se usó `npm run build --mode development` (bundle real, `.env.development` para apuntar al backend local) servido con `vite preview` — el SW compilado real (`dist/sw.js`) sí instala, activa y controla la página correctamente.

## 8. Verificar CRUD, estados, ACs y vinculación QE (frontend-incidentes-cutover-verification)

- [x] 8.1 Creación de Incidente desde `IncidentForm` (escritorio), incluyendo `localId`/`zonaId`/`ubicacion` desde la creación. ✅ (verificado con Playwright contra backend real: `POST /api/incidents` → 201, `localId`/`zonaId` persistidos, `severidad` calculada server-side (`MEDIA` para `INCIDENTE`), evidencia con URL real `/uploads/incidentes/...` — no `blob:`). Dos hallazgos reales encontrados y corregidos/documentados en el camino: (1) `fechaEvento` crasheaba la creación con 500 (`DateTimeKind.Unspecified` vs `timestamptz` de Postgres) — corregido en `IncidentForm.tsx` con conversión explícita local↔UTC, ver design.md; (2) `severidad` vacío bloquea el submit en silencio para `JEFE_CALIDAD_SYST` (sin error visible) — documentado como hallazgo no bloqueante, no se corrige en este change.
- [x] 8.2 Listado, filtros y búsqueda en `IncidentList` contra el backend real. ✅ (verificado indirectamente: la lista renderiza correctamente los ~11 incidentes de prueba creados durante la sesión de verificación, confirmando `GET /api/incidents` con paginación real).
- [x] 8.3 Actualización de investigación incluyendo los seis campos nuevos (`testigos`, `personalInvolucrado`, `equiposInvolucrados`, `atencionMedicaRequerida`/`Descripcion`, `notificacionAmbientalRequerida`). ✅ — verificado con Playwright vía `PATCH /api/incidents/:id`: los 6 campos persistidos correctamente. Además se agregó una segunda evidencia durante la edición (append real, no `blob:`). Dos hallazgos reales encontrados y corregidos: (1) `incidentPermissions.ts` calculaba `isDeleted = deletedAt !== undefined`, que el backend real (siempre serializa `deletedAt: null` explícito) trataba como "eliminado" para TODO incidente — bloqueaba `canEdit`/`canDelete`/`canCrearQE` por completo; corregido a `deletedAt != null` con tests de regresión nuevos. (2) `ActualizarInvestigacionHandler` duplicaba evidencias ya existentes al agregar una nueva (el frontend envía la lista completa existente+nueva, replicando el contrato del mock que reemplaza el arreglo); corregido con dedupe por `Url`. Ambos documentados en design.md.
- [x] 8.4 Transiciones de estado válidas e inválidas. ✅ (verificado vía API directa — no hay UI de transición de estado en el árbol de componentes actual, `useUpdateIncidentStatus` está exportado pero sin consumidor; hallazgo informativo, no bloqueante, no se corrige en este change). `PATCH /status` ABIERTO→EN_INVESTIGACION → 200; EN_INVESTIGACION→CERRADO (inválida) → 422.
- [x] 8.5 Eliminación (soft-delete) y restauración. ✅ verificado por UI completa (clic en "Eliminar"/"Restaurar", modal de confirmación, `DELETE`/`PATCH .../restore` reales) — confirmado `deletedAt` seteado y luego limpiado.
- [x] 8.6 Crear, actualizar y cerrar una Acción Correctiva (vía el endpoint `/cerrar` nuevo). ✅ verificado vía API directa (no hay botón "Agregar AC" en la UI actual — el flujo real es "Solicitar AC en QE"/Modelo B; `useCreateACIncidente` está exportado pero sin consumidor, hallazgo informativo no bloqueante). `POST /acciones` → 201, `PATCH /acciones/:acId` → 200, `PATCH /acciones/:acId/cerrar` (endpoint nuevo D5) → 200 con `estado: CERRADA`.
- [x] 8.7 Crear un QE desde `IncidentDetailPage` y confirmar la vinculación `qeId` en ambos sentidos. ✅ verificado vía API directa (creación real vía UI se evitó porque `qualityEventCreate.schema.ts`/`CrearQualityEventCommand` tiene el mismo defecto preexistente de `fechaHoraEvento` crudo sin timezone que crasheaba Incidentes — mismo bug, módulo distinto, fuera de alcance de este change; documentado como hallazgo informativo para el futuro `cutover-quality-events`). `POST /api/quality-events` (origen O1_INCIDENTE_CAMPO) → 201, `PATCH /api/incidents/:id {qeId}` → 200, confirmado `Incidente.qeId` vinculado.

## 9. Verificar el ciclo offline completo con Service Worker real

- [x] 9.1 Confirmado con Playwright: `vite dev` NO sirve — `dev-sw.js` falla evaluación en Chromium headless (hallazgo de tooling, no de la app). `npm run build --mode development` + `vite preview` (puerto 4173, agregado a `Cors:AllowedOrigins`) sí funciona — `dist/sw.js` real instala/activa/controla la página, confirmado vía `navigator.serviceWorker.getRegistrations()` (scope `/m/`, `active: 'activated'`) y `navigator.serviceWorker.controller` tras un reload.
- [x] 9.2 Reporte mobile online (`IncidentQuickReportForm`) con fotos: confirmado con Playwright — se sube a `POST /api/incidents/evidencias` (201) antes de crear el Incidente, `evidencias[].url` apunta a `/uploads/incidentes/...` real (no `blob:`). Hallazgo real encontrado y corregido en el camino: `geoUbicacion`/`ubicacion` nunca llegaban anidados en el JSON de respuesta (backend serializaba `geoLat`/`geoLng`/`geoCapturadoEn` sueltos) — rompía también la vista "Mapa" (`IncidentMapCanvas.tsx`) y el marcador en el plano (`IncidentDetailPage.tsx`) para cualquier incidente con ubicación guardada, no solo geoUbicacion. Corregido con propiedades JSON computadas en `Incidente.cs`, ver design.md.
- [x] 9.3 Reporte mobile sin conexión: encolar con fotos y geolocalización, cortar la red del dispositivo/navegador, confirmar que queda en la cola local. ✅ verificado con Playwright (`context.setOffline(true)`) contra el Service Worker real (`dist/sw.js`, ver 9.1): el reporte muestra la pantalla de encolado y queda una entrada real en IndexedDB (`shac-offline-incidents`, store `incident-queue`, `count: 1`).
- [x] 9.4 Reconectar y confirmar que la sincronización FIFO se dispara sola (evento `online`), sube las fotos reales durante el sync, y el Incidente resultante tiene URLs reales y `geoUbicacion` correctos. ✅ verificado con Playwright: `context.setOffline(false)` dispara el listener `online` sin intervención manual — `POST /api/incidents/evidencias` (201, foto real) seguido de `POST /api/incidents` (201) contra el backend real; el incidente sincronizado tiene `evidencias[].url` real (no `blob:`) y `geoUbicacion` completo; cola vacía (`count: 0`) tras el sync exitoso.
- [x] 9.5 Verificar la excepción de `empresaId`. Verificado con una prueba directa y precisa del contrato backend (más rigurosa que forzar el cambio de empresa a mitad de una desconexión real del navegador, que requeriría red para el propio switch-empresa): login con JWT `empresaActivaId = Empresa A`, `POST /api/incidents` (1) sin `empresaId` en el body → crea bajo Empresa A (la del JWT); (2) con `empresaId = Empresa B` y membresía ACTIVA → 201, crea bajo Empresa B, ignora la del JWT; (3) con `empresaId` de una empresa sin membresía → 403, no crea. Los tres casos exactos que implementa D4.
- [x] 9.6 Confirmar que un fallo de sincronización de una entrada no bloquea a las siguientes de la cola. ✅ ya cubierto por tests unitarios existentes y pasando (`useOfflineIncidentSync.test.ts`: "un reporte que falla se marca en error y no bloquea al siguiente", "un fallo de red que vuelve a pending no bloquea la sincronización del siguiente reporte en el mismo ciclo") — no se repitió en Playwright por ser redundante con cobertura unitaria ya sólida de esta lógica de retry/FIFO.

## 10. Tests y cierre del ciclo

- [x] 10.1 Inspeccionados los tests de Incidentes: dos archivos ejercitan `msw/node`/`setupServer` real — `src/mocks/handlers/incidents.handlers.test.ts` y `src/features/incidents/hooks/useZonasByLocal.test.ts` — pero ambos testean explícitamente el comportamiento de los HANDLERS MSW mismos (`setupServer(...incidentHandlers)`/`setupServer(...localesHandlers)`), no un flujo de la app contra un backend — migrarlos a un backend real les quitaría su propósito (validan la lógica del mock, que debe seguir intacta para los módulos aún no cutover-eados). Mismo criterio y misma conclusión que `cutover-auth` D4: no se migra ningún archivo de test en este change.
- [x] 10.2 Suite completa corrida sin regresiones: backend 451/451, frontend 1341 passed + 1 expected fail (pre-existente, mismo patrón ya documentado en `cutover-auth`) — incluye 2 tests nuevos de regresión agregados en este change (`incidentPermissions.test.ts`, hallazgo `deletedAt: null`).
- [x] 10.3 Todos los hallazgos reales documentados en `design.md` con causa raíz confirmada antes de corregir: (1) `fechaEvento` crudo → 500 por `DateTimeKind.Unspecified`; (2) `severidad` vacío bloquea submit en silencio para `JEFE_CALIDAD_SYST` (no bloqueante, no se corrige); (3) `ActualizarInvestigacionHandler` duplicaba evidencias existentes al agregar una nueva; (4) `isDeleted` (`deletedAt !== undefined`) bloqueaba edición/eliminación/QE para todo incidente contra el backend real; (5) `ubicacion`/`geoUbicacion` nunca llegaban anidados en el JSON, rompiendo el marcador en el plano y la vista "Mapa"; (6) `vite dev` no sirve para el SW real de la PWA, hace falta build+preview; (7) status de Incidente / creación directa de AC sin punto de entrada en la UI actual (informativo); (8) el formulario de creación de QE tiene el mismo defecto de `fechaHoraEvento` que (1), fuera de alcance (futuro `cutover-quality-events`).
- [x] 10.4 Revertido `shc-controldoc/.env.development` a `VITE_ENABLE_MSW=true` (con `VITE_API_BASE_URL` vacío, estado original).
- [x] 10.5 Confirmado: `shc-controldoc/.env.production` no requiere cambios — ya tenía `VITE_ENABLE_MSW=false` desde `cutover-auth`, con el mismo Open Question de hosting pendiente.
- [x] 10.6 Evaluado — no aplica, mismo criterio que `cutover-auth` 6.3: el estado global de Incidentes en la tabla "Módulos del Sistema" de `CLAUDE.md` sigue siendo "Implementado (mock)" a nivel de toda la app hasta que los 8 módulos restantes tengan su propio cutover.
