## 1. Preparar el entorno local

- [x] 1.1 Confirmar `docker-compose up -d` en `ShcMvpEndPoint/` con `shac-postgres` sano — confirmado, `shac-postgres` estaba `Up 11 days (healthy)`. Nota: `ShcMvpEndPoint/` vive dentro de este mismo repo (`ShcMvp/ShcMvpEndPoint`), no en un checkout separado — corrige una suposición incorrecta de `design.md` al proponer este change (ver nota actualizada en `design.md` Context).
- [x] 1.2 Confirmar `dotnet run` levanta sin errores contra Postgres dev, anotar el puerto real; confirmar que responde 401 en `/api/quality-events` sin token — confirmado, `http://localhost:5072` (perfil `http`), 401 sin token. Arranque lento (~1 minuto): `DevSeedAreasExtensions` re-siembra áreas por cada empresa dev existente vía N+1 queries, generando cientos de miles de líneas de log EF Core en `Information` — mismo patrón ya documentado en `cutover-incidentes`/`cutover-no-conformidades` 1.2, no bloqueante para este change.
- [x] 1.3 Confirmar que los usuarios QA reutilizables cubren los roles necesarios — confirmado por login directo: `multi.empresa.qa@shac.dev`/`QaTest_2026!` (mono-empresa, rol `JEFE_CALIDAD_SYST` en "QA Empresa Beta S.A.C.") y `sin.empresa.qa@shac.dev`/`QaTest_2026!` (mono-empresa, rol `SUPERVISOR`, **misma empresa** "QA Empresa Beta S.A.C.") — cubren la firma dual completa (primera firma JEFE_CALIDAD_SYST + segunda firma SUPERVISOR) dentro de una sola empresa, sin necesitar selección de empresa activa. `ALTA_DIRECCION` no se configura: `resolveRolSegundaFirma` (frontend, confirmado `qualityEventPermissions.ts:155-160`) siempre retorna `'SUPERVISOR'` desde D15/RN-QE-004 (2026-07-17) — la rama `ALTA_DIRECCION` es código muerto inalcanzable, no hace falta un usuario para probarla.
- [x] 1.4 Inspeccionar los tests de Quality Events que referencien `'1234'`, `accion`/`estado` de ajuste de plazo, `PinModal`, `revisarAjustePlazoAC`, o `causaRaizFirmadaEn` — documentar qué asumen antes de tocar el código, para no romperlos a ciegas.

## 2. Frontend — corregir el PIN hardcodeado del cierre (D1)

- [x] 2.1 `QECierreSection.tsx`, `PinModal.handleSubmit`: reemplazado `if (pin !== '1234')` por `if (!/^\d{4}$/.test(pin))`, sin comparar contra ningún valor correcto. `onConfirm(pin)` se llama con cualquier PIN bien formado.
- [x] 2.2 Confirmado sin cambios — `useFirmarCierre`'s `onError` (`useFirmarCierre.ts:30-34`) ya muestra el mensaje del backend en un 401.
- [x] 2.3 Confirmado — cero referencias restantes a la comparación `pin !== '1234'` en `QECierreSection.tsx` (grep).

## 3. Frontend — corregir el mismatch de `revisarAjustePlazoAC` (D2) y el bug de caché de ajuste de plazo (D4)

- [x] 3.1 `quality-events.api.ts`, función `revisarAjustePlazoAC`: mapea internamente `accion: 'APROBAR' | 'RECHAZAR'` al body HTTP real `{ estado: 'APROBADA' | 'RECHAZADA', comentarioRevision }` antes de llamar `api.patch`. La firma pública de la función no cambió.
- [x] 3.2 Confirmado sin cambios necesarios — `useRevisarAjustePlazoAC.ts` y `QEACSection.tsx` (líneas 663-664, 676-680) siguen invocando con `{ accion: 'APROBAR' | 'RECHAZAR', comentarioRevision? }`.
- [x] 3.3 (D4, Hallazgo 4 — encontrado durante la implementación) `quality-events.api.ts`: cambiado el tipo de retorno declarado de `solicitarAjustePlazoAC` y `revisarAjustePlazoAC` de `Promise<QualityEvent>` a `Promise<AccionCorrectivaQE>` (`api.post<AccionCorrectivaQE>`/`api.patch<AccionCorrectivaQE>`), coincidiendo con lo que el backend real serializa (`SolicitarAjustePlazoACHandler`/`RevisarAjustePlazoACHandler`, confirmados `Task<AccionCorrectivaQE>` leyendo el código fuente directamente).
- [x] 3.4 `useSolicitarAjustePlazoAC.ts`: `onSuccess` cambiado de `setQueryData(...)` con el resultado de la mutación a `invalidateQueries({ queryKey: QE_QUERY_KEYS.detail(qeId) })` + `invalidateQueries({ queryKey: QE_AUDIT_TRAIL_QUERY_KEY(qeId) })` — mismo patrón que `useCerrarQEAccion.ts`/`useUpdateQEAccion.ts`.
- [x] 3.5 `useRevisarAjustePlazoAC.ts`: mismo fix que 3.4.
- [x] 3.6 (D5, hallazgo encontrado al planificar D2/D4) `quality-events.handlers.ts`, handler `PATCH .../solicitud-plazo/:solicitudId`: ahora lee `body.estado` (`'APROBADA' | 'RECHAZADA'`) en vez de `body.accion`, misma bifurcación de comportamiento; responde con la `AccionCorrectivaQE` actualizada (no el `QualityEvent` completo) envuelta en `ApiResponse`.
- [x] 3.7 (D5, hallazgo preexistente encontrado en el mismo archivo, no introducido por este change) `quality-events.handlers.ts`, handler `POST .../solicitud-plazo`: ahora responde con la `AccionCorrectivaQE` actualizada (no el `QualityEvent` completo) — alinea el handler con su propia spec ya vigente (`quality-event-msw-handlers/spec.md:348`, que ya decía esto) y con D4.
- [x] 3.8 `npx tsc -b --force` sin errores nuevos tras los cambios de 3.1-3.7 — confirmado: único error remanente es el preexistente y no relacionado de `features/incidents/components/IncidentForm.tsx` (ya documentado en `cutover-documentos`).

## 4. Investigar y corregir el PIN hardcodeado de causa raíz (D3, Hallazgo 3)

- [x] 4.1 Con el backend local corriendo (paso 1.2), revisar `EndpointExtensions.cs` completo en busca de cualquier endpoint de firma dedicado para la aprobación de causa raíz de un QE — **confirmado que NO existe**: las 22 rutas registradas bajo "// Quality Events" (`EndpointExtensions.cs:205-228`) no incluyen ningún endpoint de firma de causa raíz; solo `FirmarCierreQE` existe como endpoint de firma real en todo el módulo.
- [x] 4.2 `QEInvestigationSection.tsx`, `PinModal.handleSubmit` — mismo fix que 2.1 (`if (!/^\d{4}$/.test(pin))`, sin comparar contra un valor correcto). `onPinConfirm` sigue llamando `useUpdateQualityEvent().mutate(...)` sin cambios adicionales (el PIN sigue sin viajar en el payload, documentado explícitamente como confirmación de UX no verificada server-side en la spec delta de este change).
- [x] 4.3 Confirmado — cero referencias restantes a la comparación `pin !== '1234'` en todo `shc-controldoc/src` fuera de tests (grep).

## 5. Apuntar el frontend al backend real

- [x] 5.1 Editado `shc-controldoc/.env.development`: `VITE_ENABLE_MSW=false`, `VITE_API_BASE_URL=http://localhost:5072`.
- [x] 5.2 `npm run dev` sirve (`http://localhost:5174` — `5173` ocupado, Vite tomó el siguiente puerto libre; `5174` ya está en `Cors:AllowedOrigins` de `appsettings.Development.json`), `HTTP 200`. **Nota de metodología (esta sesión no tiene herramienta de automatización de navegador disponible)**: la Sección 6 se verifica vía llamadas HTTP directas (`curl`) contra `http://localhost:5072`, con JWT real obtenido por login — mismo criterio que `cutover-no-conformidades`/`cutover-documentos`.

## 6. Verificar el ciclo de vida completo de un Quality Event (frontend-quality-events-cutover-verification)

- [x] 6.1 `POST /api/quality-events` (origen `O3_HALLAZGO_AUDITORIA`, area `area-001`) → 201, `numero: "QE-2026-008"` generado server-side, `estado: 'ABIERTO'`. `GET /api/quality-events?page=1&pageSize=5` → 200, `totalItems: 8` (incluye el nuevo QE).
- [x] 6.2 Transición de estados verificada: `ABIERTO → EN_INVESTIGACION → ANALISIS_COMPLETADO` (200 cada una). RN-QE-002 confirmado: `PATCH /status {nuevoEstado: 'EN_EJECUCION'}` sin causa raíz firmada → **422** `"RN-QE-002: la causa raíz debe estar definida y firmada antes de pasar a EN_EJECUCION"`; tras aprobar causa raíz (6.3) el mismo request → 200. RN-QE-009 (precisión sobre la redacción original de esta tarea: bloquea por solicitudes de **creación** de AC pendientes vía `solicitar-ac`, no por solicitudes de ajuste de plazo — dos conceptos distintos) verificado end-to-end con un QE dedicado (QE-2026-012): `PATCH /solicitar-ac` → 200 (`solicitudesAC: 1`); `PATCH /status {nuevoEstado: 'ANALISIS_COMPLETADO'}` → **422** `"RN-QE-009: hay solicitudes de acción correctiva pendientes"`. RN-QE-003 ver 6.4/6.8.
- [x] 6.3 Aprobación de causa raíz verificada end-to-end con el flujo ya corregido (D3): `PinModal` ya no compara contra `'1234'` (validación de formato solo, confirmado por lectura del código tras el fix) — la confirmación dispara el mismo `PATCH /api/quality-events/:id` genérico que se ejecutó aquí directamente (`{ causaRaizDefinitiva, causaRaizAprobadaPorId, causaRaizFirmadaEn }`) → 200, con `causaRaizFirmadaEn`/`causaRaizAprobadaPorId` seteados — confirmado que habilita la transición a `EN_EJECUCION` (ver 6.2). No se probó el PIN mal formado por UI (sin navegador en esta sesión) — el fix de formato (`/^\d{4}$/`) se verificó por lectura de código, no por interacción.
- [x] 6.4 AC creada (`POST .../acciones-correctivas`, responsable `SUPERVISOR`) → 201, `estado: 'PENDIENTE'`. `PATCH .../status {estado: 'EN_EJECUCION'}` → 200. Cierre con evidencia y auto-transición del QE: ver 6.8 (se completó junto con el flujo de cierre, ambas ACs cerradas antes de `PENDIENTE_CIERRE`).
- [x] 6.5 **Confirmado el fix de D2/D4 end-to-end contra el backend real**: `solicitarAjustePlazoAC` (`fechaSolicitada: 2026-11-15`) → 201, `solicitud.estado: 'PENDIENTE'`, `requiereAprobacionGerencia: false`. `revisarAjustePlazoAC` con el payload **corregido** `{ estado: 'APROBADA' }` (no el `{ accion: 'APROBAR' }` roto original) → 200, respuesta con forma de `AccionCorrectivaQE` (confirma D4): `plazoFecha` pasó de `2026-10-15` a `2026-11-15`, `solicitud.estado: 'APROBADA'` con `revisadoPorId`/`revisadoEn` seteados — el bug original (Hallazgo 2) quedaría en `solicitud.estado: 'PENDIENTE'` para siempre con el payload viejo; no se re-probó el payload roto para no gastar la única solicitud `PENDIENTE` disponible, pero el comportamiento server-side ya está confirmado por lectura directa de `RevisarAjustePlazoACHandler.cs` (ver design.md Hallazgo 2). Reintentar revisar la misma solicitud ya `APROBADA` → **422** `"la solicitud de ajuste de plazo ya fue revisada"` (el guard de reintento sí dispara ahora que `Estado` cambia de verdad).
- [x] 6.6 Repetido con una segunda AC/solicitud y **rechazo**: `{ estado: 'RECHAZADA', comentarioRevision: 'Plazo insuficientemente justificado para esta prioridad.' }` → 200, `solicitud.estado: 'RECHAZADA'` con el comentario guardado, `plazoFecha` sin cambios (`2026-10-20`, igual al valor original de creación de la AC).
- [x] 6.7 Edición de reporte inicial, severidad y mineral involucrado verificadas contra el backend real: `PATCH /:id/editar-mineral {mineralInvolucrado: 'Cobre'}` → 200. `PATCH /:id/editar-severidad {severidad: 'CRITICA'}` → 200 (y de nuevo a `'ALTA'`) — confirmado que la respuesta es el `QualityEvent` plano, **sin** el campo `requiereNotificacionUrgente` que la investigación previa afirmaba que el handler devolvía (`EditarSeveridadQEHandler.cs` retorna `Task<QualityEvent>`, no un DTO extendido) — corrección informativa a esa parte de la investigación previa, sin impacto porque ningún componente lo consume (ya confirmado por Cowork); la notificación best-effort en el `try/catch` para `CRITICA` sí es real y correcta. `PATCH /:id/editar-reporte-inicial` sobre un QE `CERRADO` → 422 `"RN-QE-014: solo se puede editar el reporte inicial mientras el QE está ABIERTO"` (confirma el gate); repetido sobre un QE nuevo en `ABIERTO` (`descripcion`, `hallazgoCodigo`, `normativaVinculada`) → 200, los tres campos persistidos y auditados (`REPORTE_INICIAL_EDITADO`).
- [x] 6.8 Cierre con firma dual verificado end-to-end contra el backend real: `PATCH /:id/cerrar` bloqueado 422 con AC pendiente (RN-QE-003), 200 tras cerrar la AC con evidencia. Primera firma (`JEFE_CALIDAD_SYST`, PIN `5150`) con PIN incorrecto (`0000`) → 401 `"PIN incorrecto"`; con PIN correcto → 200, `estado` sigue `PENDIENTE_CIERRE`, `cerradoPorId` seteado. Segunda firma (`SUPERVISOR`, PIN `7331`, distinto del primero) con PIN incorrecto (`9999`) → 401; con PIN correcto → 200, `estado: 'CERRADO'`. **Hallazgo real encontrado aquí** (Hallazgo 5/D6, documentado en `design.md`): `fechaVerificacionProgramada` quedaba `null` tras la segunda firma pese a `estado: 'CERRADO'` — `FirmarCierreQEHandler.cs` nunca la calculaba. Corregido (`qe.FechaVerificacionProgramada = now.AddDays(qe.PlazoVerificacionDias ?? 60)`), re-verificación en 6.8b. `fechaCierre` permanece `undefined` en la respuesta — confirmado intencional (columna inexistente en el backend real, derivada solo para Dashboard vía `QEFechaDerivadaResolver`), documentado como informativo/fuera de alcance en `design.md`.
- [x] 6.8b Re-verificación puntual del fix de D6, en dos pasadas: (1) QE-2026-009, con el backend aún corriendo el binario viejo (`dotnet run` no recompila solo — hace falta reiniciar el proceso, no alcanza con guardar el archivo) → `fechaVerificacionProgramada` seguía `null` tras la firma dual, confirmando por qué el bug pasó desapercibido hasta ahora. (2) Backend detenido y reiniciado (recompila); QE-2026-010 (`transición directa EN_EJECUCION → PENDIENTE_CIERRE` sin ACs, permitida por el endpoint de transición genérico — RN-QE-003 solo se exige en `/cerrar`, no en `/status`, comportamiento intencional ya documentado en `CerrarQEHandler.cs`) llevado hasta la firma dual completa → **200, `fechaVerificacionProgramada: '2026-11-06T14:49:05Z'`**, exactamente `actualizadoEn` (`2026-09-22`) + `plazoVerificacionDias: 45` días. Confirma el fix D6 con evidencia real end-to-end, no solo por lectura de código.
- [x] 6.9 `POST /:id/export-pdf` contra el backend real → **`204 No Content`, sin cuerpo** — contradice la conclusión de la investigación previa ("no es una discrepancia"). **Hallazgo real encontrado aquí** (Hallazgo 6/D7, ver Sección 8 y `design.md`): con el código previo al fix, esto rompía tanto la exportación individual (`QEHeaderSection.tsx`) como en lote (`exportQualityEventsBatch.ts`) con un `TypeError` sobre `undefined`. Corregido; ver Sección 8 para el detalle del fix y su verificación (tests, no navegador).
- [x] 6.10 Verificado campo por campo (`VerificacionEficaciaQECommand(QEResultadoVerificacion Resultado, string Evidencia)` y `ForzarVencimientoVerificacionCommand(Guid? AuditorAsignadoId)`, ambos coinciden con los schemas de frontend) y exercised end-to-end contra el backend real: `POST /:id/verificacion-eficacia {resultado: 'NO_EFECTIVO', evidencia}` → `estado: 'EN_INVESTIGACION'`, `ciclo: 2` (reapertura real, RN-QE-007 confirmado — el ciclo se incrementa). `{resultado: 'EFECTIVO', evidencia}` sobre un QE distinto → `estado: 'VERIFICADO'` (terminal), `ciclo` sin cambios. `PATCH /:id/forzar-vencimiento-verificacion {}` sobre un tercer QE → mismo patrón de reapertura (`EN_INVESTIGACION`, `ciclo: 2`), confirmando que usa el mismo `QEReaperturaHelper.Reabrir` que la verificación `NO_EFECTIVO`. **Hallazgo informativo, no bloqueante**: `ForzarVencimientoVerificacionCommand.AuditorAsignadoId` no se usa en ningún punto de `ForzarVencimientoVerificacionHandler.cs` (confirmado por grep en todo el backend) — el campo se acepta pero se descompone al vacío; `QualityEvent` (entidad backend) tampoco tiene ninguna propiedad `AuditorAsignadoId`. Es un endpoint dev-only (comentario propio del código: "Gateado a IsDevelopment(), nunca expuesto en producción"), sin impacto de producción — no se corrige en este change.
- [x] 6.11 Vinculación con Documentos verificada: `POST /:id/documentos-vinculados {documentoId}` → 200, `documentosVinculados` incluye el documento. `DELETE /:id/documentos-vinculados/:docId` → 200, `documentosVinculados: []`. Sin regresión respecto a la verificación previa de `documento-qe-vinculacion`.
- [x] 6.12 `DELETE /api/quality-events/:id` → 200, `deletedAt` seteado a un timestamp real. `PATCH /:id/reactivar` → 200, `deletedAt: null`. Confirmado con re-login (el JWT de 60 minutos había expirado durante la sesión de verificación — se refrescaron los tokens QA).

## 8. Corregir la exportación de PDF, individual y en lote (D7, Hallazgo 6)

- [x] 8.1 `quality-events.api.ts`: `exportQualityEventPdf(id)` cambia de `Promise<QualityEvent>` a `Promise<void>` — `await api.post(...)` sin tipar ni devolver `response.data` (el backend responde 204 sin cuerpo).
- [x] 8.2 `useExportQualityEventPdf.ts`: `onSuccess` cambia de `setQueryData` con el resultado (ya no existe) a `invalidateQueries({ queryKey: QE_AUDIT_TRAIL_QUERY_KEY(id) })`.
- [x] 8.3 `QEHeaderSection.tsx`, `handleExportPdf`: arma el PDF con el `qe` que ya llega como prop del componente, no con el resultado de la mutación.
- [x] 8.4 `exportQualityEventsBatch.ts`: agrega `getQualityEvent(qeId)` (`Promise.all` junto con `exportQualityEventPdf(qeId)`) por cada QE del lote; el PDF y el nombre de archivo del ZIP se arman con el QE obtenido por GET.
- [x] 8.5 Tests actualizados: `QEHeaderSection.test.tsx` (la mutación ya no pasa un QE a `onSuccess`; se verifica que `buildQualityEventPdf` recibe el `qe` prop) y `exportQualityEventsBatch.test.ts` (mock de `getQualityEvent` agregado).
- [x] 8.6 `npx vitest run src/features/quality-events` → 247/247 tests pasando (30 archivos), sin regresiones. `npx tsc -b --force` → limpio salvo el error preexistente ya documentado de `IncidentForm.tsx`.

## 7. Tests y cierre del ciclo

- [x] 7.1 Todos los hallazgos reales (Hallazgos 1-6, D1-D7) documentados en `design.md` con causa raíz confirmada antes de corregir. RN-QE-009 (6.2) y `VerificacionEficaciaQEHandler`/`ForzarVencimientoVerificacionHandler` (6.10) verificados end-to-end (no solo por lectura de código).
- [x] 7.2 Backend: `dotnet test` → **451/451 pasando** (incluye la aserción nueva de D6 en `QualityEventsCierreEndpointTests.cs`), sin regresiones. Frontend: `npx tsc -b --force` → limpio salvo el error preexistente y no relacionado de `IncidentForm.tsx` (ya documentado en `cutover-documentos`). `npx vitest run` → **168/168 archivos, 1310 passed + 1 expected fail** (mismo expected-fail preexistente ya documentado en `cutover-auth`/`cutover-incidentes`/`cutover-documentos`) — sin regresiones.
- [x] 7.3 Revertido `shc-controldoc/.env.development` a `VITE_API_BASE_URL=` (vacío) / `VITE_ENABLE_MSW=true`, estado original.
- [x] 7.4 Confirmado — `shc-controldoc/.env.production` no requiere cambios, ya tenía `VITE_ENABLE_MSW=false` desde `cutover-auth`, mismo Open Question de hosting pendiente.
- [x] 7.5 Evaluado — no aplica, mismo criterio que cutovers anteriores: el estado global de Quality Events en la tabla "Módulos del Sistema" de `CLAUDE.md` sigue siendo "Implementado (mock)" a nivel de toda la app hasta que los módulos restantes (Notifications/Dashboard, Users) tengan su propio cutover.
