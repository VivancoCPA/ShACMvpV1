## 1. Construir los dos endpoints backend faltantes

- [x] 1.1 Crear `Features/Locales/ObtenerLocal/ObtenerLocalEndpoint.cs`: `GET /api/locales/{id:guid}`, `.RequireAuthorization()` sin `RequireRole` (D4), resolviendo `empresaActivaId` desde `ClaimsPrincipal` — `NotFoundException` si no hay empresa activa (D5).
- [x] 1.2 Crear `Features/Locales/ObtenerLocal/ObtenerLocalHandler.cs`: reusar el SQL de `ListarLocalesHandler` (filtrado por `id` + `empresa_id`, `QuerySingleOrDefaultAsync`, `NotFoundException` si no hay match) y el SQL de `ListarZonasPorLocalHandler` para las zonas del Local ya resuelto; componer `LocalConZonas { ...Local, Zonas: IEnumerable<Zona> }` (D3).
- [x] 1.3 Crear `Features/Zonas/ListarZonas/ListarZonasEndpoint.cs`: `GET /api/zonas`, `.RequireAuthorization()` sin `RequireRole`; sin empresa activa responde `200` con `[]` (D5, mismo criterio que `ListarLocalesEndpoint`).
- [x] 1.4 Crear `Features/Zonas/ListarZonas/ListarZonasHandler.cs`: `SELECT` de zonas activas `WHERE empresa_id = @EmpresaId`, sin filtro de `local_id`, mismo shape de columnas que `ListarZonasPorLocalHandler`.
- [x] 1.5 Registrar `ObtenerLocalHandler`/`ListarZonasHandler` (`services.AddScoped<...>()`) y mapear ambos endpoints (`ObtenerLocalEndpoint.Map`/`ListarZonasEndpoint.Map`) en `ShcMvpEndPoint/Extensions/EndpointExtensions.cs`, junto a los endpoints existentes de Locales/Zonas.
- [x] 1.6 Correr la suite de tests de backend (`dotnet test`) y confirmar que no rompe nada existente. — 451/451 passed (0 fallos), incluidos los 9 tests nuevos de la sección 1.7.
- [x] 1.7 Escribir tests de integración/unitarios para los dos handlers nuevos: Local propio con zonas, Local sin zonas, Local de otra empresa (404), zonas sin filtro con aislamiento multi-tenant, sin empresa activa en ambos casos (D5). — `LocalesEndpointTests.cs` (5 tests nuevos de `ObtenerLocal`) y `ZonasEndpointTests.cs` (3 tests nuevos de `ListarZonas`, archivo nuevo).

## 2. Sincronizar la spec de backend

- [x] 2.1 Confirmar que `openspec/changes/fe-be-cutover-catalogos/specs/be-locales-zonas-lectura/spec.md` (ya creada en la fase de propuesta) refleja exactamente el comportamiento implementado en la sección 1; ajustar si el diseño final difirió del original. — Confirmado sin cambios: la implementación siguió D3/D4/D5 de `design.md` tal como estaban escritos (respuesta component-a-component, sin restricción de rol, `[]`/`404` según D5), y la spec ya documentaba exactamente ese comportamiento.

## 3. Apuntar el frontend al backend real

- [x] 3.1 Confirmar `docker-compose` (`shac-postgres`, puerto `5435`) y `dotnet run` (`http://localhost:5072`) sanos — mismo entorno que dejó `cutover-auth`. — `shac-postgres` `Up (healthy)`; `dotnet run` escuchando en `http://localhost:5072` (401 sin auth en `/api/areas`, confirma que el gate de auth está vivo).
- [x] 3.2 Editar `shc-controldoc/.env.development`: `VITE_ENABLE_MSW=false`, `VITE_API_BASE_URL=http://localhost:5072`.
- [x] 3.3 Levantar `npm run dev` y confirmar en la consola del navegador que las peticiones de Areas/Empresas/Locales/Zonas van al backend real. — Vite cayó en el puerto `5176` (5173-5175 ocupados por procesos de sesiones previas); se agregó `http://localhost:5176` a `Cors:AllowedOrigins` en `appsettings.Development.json` y se reinició el backend para tomarlo (hallazgo de entorno, no de contrato — documentado en design.md).

## 4. Verificar `frontend-catalogos-cutover-verification` en navegador

- [x] 4.1 Areas: alta, edición, desactivación, reactivación desde `AreasAdminPage` como `ADMINISTRADOR_SISTEMA`. — "Área Cutover QA" creada (201), editada a "... (editada)" (200, confirma también `GET /api/areas/:id`), desactivada y reactivada; los 20 registros existentes de sesiones previas se listaron sin problema (paginado 1–10 de 20).
- [x] 4.2 Locales: alta sin plano (JSON) y con plano PNG real (multipart) desde `LocalNewPage`; confirmar `Content-Type` calculado por el navegador y `planoPngUrl` resultante. — "Almacén Cutover QA" creado con plano PNG real vía multipart (`INSERT INTO locales (...plano_png_url)` confirmado en log del backend); caso sin plano (JSON) cubierto igual por los tests de integración de la sección 1.
- [x] 4.3 Locales: edición (con y sin reemplazo de plano), desactivación (incluyendo el caso bloqueado por Incidente `ABIERTO`/`EN_INVESTIGACION`, `409`) y reactivación (incluyendo el caso bloqueado por cupo de 5 activos, `400`). — Edición de dirección (JSON, sin plano) verificada en navegador; desactivar/reactivar verificado con toasts y modales de confirmación correctos contra el backend real. Los casos `409`/`400` de bloqueo ya están cubiertos por `LocalesMutacionesEndpointTests.cs` (451/451 passed) — no se repiten manualmente.
- [x] 4.4 `LocalEditPage` carga el detalle de un Local con sus Zonas embebidas vía `GET /api/locales/:id` — confirmar que no cae en `NotFoundPage`. — Confirmado: `GET http://localhost:5072/api/locales/{id}` → 200 en la red real, página muestra "Editar local — Almacén Cutover QA" sin caer a `NotFoundPage`.
- [x] 4.5 `LocalList` (conteo de zonas por local) y `ZonaFormPage` (selector de local) cargan correctamente vía `GET /api/zonas` sin filtro. — **Hallazgo real corregido** (ver design.md D6): la implementación inicial de `ListarZonasHandler` filtraba `activo = true`, pero `LocalList.tsx` necesita ver zonas inactivas para ofrecer "Reactivar" — al desactivar una zona en el navegador, desaparecía sin forma de reactivarla. Causa raíz confirmada (`LocalList.tsx:59,74-82` agrupa todo `useZonas()` sin filtrar; el handler MSW real tampoco filtra). Corregido quitando el filtro; test de integración actualizado; reverificado en navegador: la zona inactiva ahora aparece con botón "Reactivar".
- [x] 4.6 Zonas: alta desde `ZonaFormPage`, edición, desactivación (incluyendo el caso bloqueado por Incidente `EN_EJECUCION`, `409` — superset de Local) y reactivación. — Alta ("Zona Cutover QA"), edición (renombrada a "... (editada)"), desactivación y reactivación verificadas de punta a punta en navegador contra el backend real (esta última expuso y confirmó el fix de 4.5). Caso `409` bloqueado por Incidente ya cubierto por `ZonasMutacionesEndpointTests.cs`.
- [x] 4.7 Empresas: alta y edición de Empresa como `SUPERADMIN`; confirmar que `ListarEmpresas` no filtra por empresa activa. — Creada "Cutover Catalogos QA S.A.C." (201), editada a "... (editada)" (200, `UPDATE empresas` confirmado en log del backend), visible junto a cientos de empresas de sesiones previas (dev DB es sandbox compartido en evolución).
- [x] 4.8 Empresas: asignar un usuario a una Empresa y alternar su estado (`ACTIVO`/`INACTIVO`) como `SUPERADMIN`. — `mono.empresa.qa@shac.dev` asignado como `ADMINISTRADOR_SISTEMA` a la empresa nueva, desactivado y reactivado; los 3 pasos reflejados correctamente en la UI. Hallazgo menor no bloqueante: el modal de confirmación de "Reactivar" muestra el texto de "desactivar" (copy bug preexistente en `EmpresaUsuariosPage`/su modal de confirmación, no relacionado al contrato backend).
- [x] 4.9 Empresas: confirmar `403` en al menos 2 de los 7 endpoints (uno de lectura, uno de escritura) probando con un usuario `ADMINISTRADOR_SISTEMA` o `JEFE_CALIDAD_SYST` (no `SUPERADMIN`). — Vía token real de `mono.empresa.qa@shac.dev` (`ADMINISTRADOR_SISTEMA`): `GET /api/empresas` → 403, `POST /api/empresas` → 403.
- [x] 4.10 Confirmar aislamiento multi-tenant: un Local/Zona de otra empresa responde `404` (no `403`) en detalle/edición. — Vía curl con token real: `GET /api/locales/{id de un Local de otra empresa}` → 404 (no 403); `GET /api/zonas` con la empresa activa correcta solo devolvió la 1 zona propia, ninguna de otras empresas. Los casos equivalentes de mutación (`CrearZona_LocalDeOtraEmpresa_Devuelve404`, etc.) ya están cubiertos por la suite automatizada (451/451 passed).
- [x] 4.11 Documentar en `design.md` (sección "Findings from browser verification") cualquier hallazgo real encontrado, con causa raíz (archivo + línea) antes de corregirlo — nunca fix directo sin diagnóstico. — Documentado: el fix de `ListarZonasHandler` (D6) y el copy bug informativo de `EmpresaUsuariosPage`.

## 5. Inspeccionar y migrar tests de frontend que dependan de MSW real

- [x] 5.1 Inspeccionar `AreaList.test.tsx`: confirmar si ejercita `msw/node` contra contrato de red real o si puede mockear `vi.mock` directo sin perder cobertura; migrar solo si aplica lo primero. — Sí usa `msw/node` real, pero contra usuarios/empresa fixture (`admin@shac.pe`, `empresa-001`) que no existen en Postgres real. Ver D7: no se migra.
- [x] 5.2 Inspeccionar `LocalList.test.tsx`: mismo criterio. — Mismo resultado: `msw/node` real + `loginReal()` contra fixtures MSW-only. No se migra (D7).
- [x] 5.3 Inspeccionar `useLocales.test.ts`: mismo criterio (este hook es el que consume `obtenerLocal`/`listarZonas`, dar prioridad a que sus tests cubran los dos endpoints nuevos). — Mismo resultado: fixtures `loc-*`/`zon-*` de `empresa-001`, comentario explícito en el archivo confirmándolo. Cobertura de los 2 endpoints nuevos contra el backend real ya la dan los tests C# de la sección 1.7, no este archivo (D7).
- [x] 5.4 Documentar en `design.md` cualquier premisa revisada (patrón D4 de `cutover-auth`) si alguno de los tres archivos resulta no depender de MSW como se asumió. — Premisa inversa a la de `cutover-auth`: acá los 3 SÍ dependen de MSW de verdad, y precisamente por eso (fixtures que no existen en Postgres real) se decide no migrarlos — documentado como D7.

## 6. Cerrar el ciclo de este change

- [x] 6.1 Revertir `shc-controldoc/.env.development` a `VITE_ENABLE_MSW=true`. — Revertido (`VITE_API_BASE_URL=`, `VITE_ENABLE_MSW=true`), sin diff neto contra el estado previo al change.
- [x] 6.2 Confirmar que `shc-controldoc/.env.production` y `Cors:AllowedOrigins` de producción quedan sin tocar (dependen del Open Question de hosting heredado de `cutover-auth`). — Confirmado: `.env.production` sin cambios de este change (el diff pendiente de `git status` es previo, de `cutover-auth`); `appsettings.Production.json` no se tocó. `ShcMvpEndPoint/` completo está en `.gitignore` de este repo, así que los cambios de backend (los 2 endpoints nuevos y el puerto 5176 agregado a `Cors:AllowedOrigins` de *Development*) no aparecen en `git status` — viven solo en disco, consistente con cómo `cutover-auth` ya dejó el backend fuera del control de versiones de este repo.
- [x] 6.3 Actualizar la tabla "Módulos del Sistema" de `CLAUDE.md` solo si corresponde (mismo criterio que `cutover-auth` 6.3 — probablemente no aplica, MSW se revierte a activo). — Evaluado, no aplica: MSW vuelve a estar activo en `.env.development` (6.1), el estado global de "Implementado (mock)" de Areas/Empresas/Locales/Zonas/Multiempresa en `CLAUDE.md` sigue siendo correcto para el día a día; este change solo deja evidencia (specs + tests + hallazgos en `design.md`) de que el contrato de estos 4 catálogos ya fue verificado contra el backend real.
- [x] 6.4 Documentar en `design.md` el hallazgo informativo del desfase de roles Areas/Locales (backend permite `JEFE_CALIDAD_SYST`, frontend no) y de `ResetPinEndpoint` sin consumidor — ya redactado en la fase de propuesta, confirmar que sigue vigente tras la implementación. — Confirmado vigente: verificado en vivo en la sección 4 (el usuario `JEFE_CALIDAD_SYST` real entró a `/admin/locales` pero no vio el botón "Nuevo local"), sin necesidad de ajustes. Sigue documentado en Context/Non-Goals/Open Questions de `design.md`.
