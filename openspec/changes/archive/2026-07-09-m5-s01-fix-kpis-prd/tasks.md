## 1. Tipos (`dashboard-types`)

- [x] 1.1 En `src/features/dashboard/types/kpi.types.ts`, agregar `KpiMetaTipo = 'ABSOLUTO' | 'REDUCCION_INTERANUAL'`; extender `KpiUnidad` con `'DISTRIBUCION'`; extender `KpiFrecuencia` con `'TIEMPO_REAL'`; agregar `metaTipo: KpiMetaTipo` a `KpiDefinition`; en `KpiResult`, extender `semaforo` con `'INFORMATIVO'` y agregar `metaTipo: KpiMetaTipo`, `valorPeriodoAnterior?: number`, `distribucion?: { area: string; valor: number }[]`.
- [x] 1.2 En `src/features/dashboard/constants/kpi.constants.ts`, reemplazar las 9 entradas de `KPI_DEFINITIONS` con los nombres/fórmulas/unidades/metas/frecuencias de SHAC-PRD-003 §5.2 (ver tabla de `design.md` §1), incluyendo `metaTipo: 'REDUCCION_INTERANUAL'` solo en `KPI-04` y `metaTipo: 'ABSOLUTO'` en las 8 restantes.

## 2. Fixture nueva para KPI-04 interanual

- [x] 2.1 Crear `src/mocks/fixtures/kpi04AnioAnterior.fixtures.ts` exportando `kpi04AnioAnteriorFixtures: { periodo: string; valor: number }[]`, con una entrada por cada período `'2026-01'`...`'2026-07'` (mismo rango que `incidents.fixtures.ts`), con valores plausibles superiores a los del período actual (para que exista margen de "reducción" real que verificar en navegador).
- [x] 2.2 Registrar el fixture nuevo en `src/mocks/fixtures/index.ts` si el patrón existente lo requiere (seguir convención de `horasTrabajadasFixtures`).

## 3. MSW handler — reescritura de fórmulas (`dashboard-msw-handlers`)

- [x] 3.0 En `src/features/dashboard/constants/kpi.constants.ts` (o `dashboard.handlers.ts`), agregar la constante `PLAZO_MAXIMO_QE_DIAS_HABILES: Record<QESeverity, number>` = `{ BAJA: 22, MEDIA: 17, ALTA: 14, CRITICA: 10 }`, derivada de sumar los plazos de SHAC-PRD-003 §1.3 (`EN_INVESTIGACION` por severidad + `ANALISIS_COMPLETADO` 2d + `PENDIENTE_CIERRE` 5d; `EN_EJECUCION` excluido — ver `design.md` decisión 6).
- [x] 3.1 En `src/mocks/handlers/dashboard.handlers.ts`, reemplazar `calcularKpi01`...`calcularKpi09` según las fórmulas corregidas de `design.md` §1: KPI-01 (tasa de cierre de QE en plazo, cutoff por severidad vía `PLAZO_MAXIMO_QE_DIAS_HABILES` — ver design.md decisión 6), KPI-02 (tiempo promedio de cierre de QE, `contarDiasHabiles`), KPI-03 (tasa de reincidencia sobre QE cerrados en el período, no sobre QE reportados), KPI-04 (mantiene fórmula de incidentes/horas, agrega comparación interanual), KPI-05 (eficacia de AC, escopado a origen QE/NC vía `resultadoVerificacion` del padre), KPI-06 (% docs vigentes sobre el total de docs `PUBLICADO`, sin filtro de período de emisión), KPI-07 (tiempo promedio de investigación con el timestamp de `auditTrail` de la transición a `ANALISIS_COMPLETADO`, no `causaRaizFirmadaEn`; `contarDiasHabiles` — ver design.md decisión 5), KPI-08 (conteo de ACs vencidas en tiempo real, sin filtro de período), KPI-09 (distribución de QE por `areaAfectada` en el período).
- [x] 3.2 Actualizar `KPI_DIRECTION` (o eliminarlo si KPI-08/KPI-09 ya no encajan en el modelo binario MAYOR_MEJOR/MENOR_MEJOR) y `calcularSemaforo` para: (a) rama especial de KPI-04 con `valorPeriodoAnterior` y umbral de reducción interanual (ver design.md §"KPI-04 calcula reducción interanual"), (b) rama especial de banda para KPI-08 (`0→VERDE`, `1-3→AMARILLO`, `>3→ROJO`), (c) `KPI-09` siempre `'INFORMATIVO'`, sin pasar por `calcularSemaforo`.
- [x] 3.3 En `calcularKpis(periodo)`, importar `kpi04AnioAnteriorFixtures`; para `KPI-04` poblar `valorPeriodoAnterior` desde el fixture por `periodo`; para `KPI-08` fijar `periodo: 'TIEMPO_REAL'` en el resultado en vez del `periodo`/`quarterInfo` recibido; para `KPI-09` poblar `distribucion` y `valor` como el conteo del área top.
- [x] 3.4 Re-evaluar `SUPERVISOR_KPI_IDS` (línea ~345): confirmar que los 5 ids actuales (`KPI-02, KPI-03, KPI-04, KPI-05, KPI-07`) siguen siendo apropiados bajo los nuevos significados (ver design.md §9 — se espera que sí, sin cambios), y ajustar `buildSupervisorData` si alguno de los 5 ahora requiere datos que esa función no está agregando (p.ej. KPI-05 necesita `resultadoVerificacion` del padre).

## 4. Tests de handlers

- [x] 4.1 Reescribir `src/mocks/handlers/dashboard.handlers.test.ts`: reemplazar todas las aserciones por id/nombre/valor de los 9 KPIs (líneas ~51-685) para reflejar las fórmulas nuevas.
- [x] 4.2 Agregar casos para KPI-04 (VERDE/AMARILLO/ROJO según reducción interanual, incluyendo el caso sin `valorPeriodoAnterior` → ROJO), KPI-08 (banda 0/1-3/>3, ignora `periodo`), KPI-09 (distribución ordenada, áreas sin QE ausentes, período vacío → `distribucion: []`), KPI-05 (AC de Incidente excluida, AC sin `resultadoVerificacion` del padre excluida del denominador), KPI-01 (cutoff distinto por severidad — un QE `CRITICA` fuera de plazo con el mismo tiempo transcurrido que un QE `BAJA` que sí cumple), y KPI-07 (usa el `timestamp` de `auditTrail` con `estadoNuevo: 'ANALISIS_COMPLETADO'` en vez de `causaRaizFirmadaEn`; QE reabierto usa la entrada más reciente; QE sin esa transición se excluye del cálculo).
- [x] 4.3 Actualizar el fixture de `qe-2026-020` u otros seeds si alguna fórmula nueva (p.ej. KPI-01/KPI-03 sobre QE cerrados en el período) deja de tener casos distintos de cero — seguir el mismo patrón de M5-S05a tarea 1.2 si es necesario. **Nota:** no fue necesario — los tests de valor exacto (4.1/4.2) se rediseñaron con datos sintéticos aislados por periodo (años 2031-2032) en vez de derivar valores a mano de los fixtures estáticos, evitando la dependencia frágil con seeds específicos.

## 5. Componente `KpiGridWidget` (`dashboard-jefecalidad-view`)

- [x] 5.1 Corregir el bug de borde en dark mode (`KpiGridWidget.tsx:48`): reemplazar `border border-hairline ... dark:border-hairline/20` por `border-y border-r border-y-hairline border-r-hairline dark:border-y-hairline/20 dark:border-r-hairline/20`, manteniendo `border-l-[3px] ${SEMAFORO_CLASSES[...]}` separado (mismo patrón ya usado en `SemaforoRow.tsx:34`).
- [x] 5.2 Agregar rama de renderizado para `metaTipo === 'REDUCCION_INTERANUAL'` (hoy solo KPI-04): mostrar el texto de meta vía una nueva clave i18n (p.ej. `jefeCalidad.kpis.metaReduccionInteranual`, "Reducción ≥{{meta}}% anual") en vez de `formatValor(kpiResult.meta, definicion.unidad)`.
- [x] 5.3 Agregar rama de renderizado para `semaforo === 'INFORMATIVO'` (hoy solo KPI-09): en vez de la tarjeta numérica genérica, renderizar las primeras 3 entradas de `distribucion` (área + conteo) en una lista compacta, con borde izquierdo neutro (no `SEMAFORO_CLASSES`) y sin `VALOR_CLASSES` de color en el texto.
- [x] 5.4 Actualizar `formatValor` para manejar `unidad: 'DISTRIBUCION'` si `KPI_DEFINITIONS['KPI-09'].unidad` se lee en algún punto fuera de la rama especial de 5.3 (evitar que el `switch` deje de ser exhaustivo en TypeScript).
- [x] 5.5 Actualizar `KpiGridWidget.test.tsx` con casos para: borde correcto en dark mode (si es testeable vía clases), KPI-04 mostrando texto de reducción interanual (no `"Meta: 10.00"`), KPI-09 mostrando lista de áreas (no un valor con `%`/`d`/tasa).

## 6. i18n

- [x] 6.1 Agregar clave `dashboard.jefeCalidad.kpis.metaReduccionInteranual` en `src/i18n/es-PE.json` y `en-US.json` (interpolando `{{meta}}`), y cualquier etiqueta necesaria para la lista de áreas de KPI-09 (p.ej. `kpis.distribucionTitulo`).

## 7. Verificación cruzada y browser verification

- [x] 7.1 Levantar la app con `VITE_ENABLE_MSW=true`, iniciar sesión como `jefe.calidad@shac.pe`, y en `/dashboard` verificar: (1) los 9 nombres/fórmulas de KPI corresponden a la tabla de SHAC-PRD-003 §5.2, (2) la tarjeta de KPI-04 muestra "Reducción ≥10% anual" y no un umbral numérico, (3) la tarjeta de KPI-09 muestra un ranking de áreas y no un porcentaje, (4) la tarjeta de KPI-08 muestra el conteo de ACs vencidas en tiempo real con semáforo de banda (0/≤3/>3), (5) el borde izquierdo de color de cada tarjeta se ve correctamente en Dark Mode. Verificado con Playwright contra el dev server (screenshots light/dark) — las 9 tarjetas, el texto de KPI-04, el ranking de KPI-09 y los bordes en dark mode se confirmaron correctos.
- [x] 7.2 Confirmar que `SupervisorDashboard` (`kpisArea`, si ya renderiza `KpiGridWidget` o llega a hacerlo en una spec futura) no quedó con nombres/fórmulas obsoletos de los 5 KPIs que expone. **Confirmado:** `SupervisorDashboard.tsx` aún no renderiza `KpiGridWidget`, no hay nombres obsoletos que corregir.
- [x] 7.3 Grep por los 9 nombres antiguos de M5-S01 (p.ej. `'Cumplimiento de Revisión Documentaria'`, `'Tasa de Reporte Proactivo SyST'`, `'Cumplimiento de Firma Dual en Cierre de QE Críticos'`) en todo `src/` para confirmar que no quedan referencias sueltas fuera de `kpi.constants.ts` (comentarios, tests, fixtures). Sin resultados — ninguna referencia suelta.
- [x] 7.4 Correr `npm run typecheck`, `npm run lint` y la suite completa de tests (`npm test`) antes de dar la spec por completa. `tsc -b`, `eslint` y `vitest run` corridos; los únicos fallos/errores restantes son preexistentes y no relacionados (archivos no tocados por este change).

## 8. Coordinación con M5-S05a

- [x] 8.1 Confirmar con el usuario si `m5-s05a-dashboard-jefecalidad` (tasks ya marcadas `[x]` pero sin archivar) debe archivarse después de aplicar este change, o si el delta de `dashboard-jefecalidad-view` de este change debe fusionarse manualmente dentro de `openspec/changes/m5-s05a-dashboard-jefecalidad/specs/dashboard-jefecalidad-view/spec.md` antes de archivar esa spec, para que lo que se sincronice a `openspec/specs/` refleje ya el comportamiento corregido. **Resuelto:** el usuario revisará ambos changes y hará el archivado manualmente (fuera de este flujo de `/opsx:apply`).
