## 1. Zod v3 → v4 error param migration (D1)

- [x] 1.1 In `src/features/documents/schemas/nuevaVersion.schema.ts`, replace `required_error` with `error` on the `tipoCambio` enum.
- [x] 1.2 In `src/features/incidents/schemas/incidentForm.schema.ts`, replace `required_error` with `error` on both the `tipo` and `turno` enums.
- [x] 1.3 In `src/features/incidents/schemas/mobileIncidentReport.schema.ts`, replace `required_error` with `error` on both the `tipo` and `turno` enums.
- [x] 1.4 Run `npx tsc -b --force` and confirm all 5 `TS2769` errors from these 3 files are gone.
- [x] 1.5 Run the existing test suite for each of the 3 affected forms/schemas (if a schema/form test exists) and confirm validation messages still render as before — flag any wording/trigger difference instead of silently accepting it.

## 2. Re-check IncidentForm.tsx after the schema fix (D2)

- [x] 2.1 Re-run `npx tsc -b --force` after Task 1 and check whether `IncidentForm.tsx`'s `TS2719`/`TS2345` "two unrelated Resolver types" errors (lines ~284, ~396) are already resolved. **Not resolved by Task 1 alone** — the real cause was independent of the `required_error` bug (see 2.2).
- [x] 2.2 Diffed `IncidentForm.tsx`'s hand-declared form-values type against the resolver's actual inferred type via `tsc --noErrorTruncation`. Root cause: `localId`/`zonaId` used `z.preprocess()`, which makes the resolver's raw-input type (`TFieldValues`) diverge from its parsed-output type (`TTransformedValues`) — `z.infer<typeof schema>` only captures the output side, so `useForm<UpdateIncidentFormInput>()` (using output for both slots) didn't match what `zodResolver()` actually produces. Fixed by (1) replacing `z.preprocess(v => v === '' ? undefined : v, z.string().optional())` with `z.string().optional().transform(v => v === '' ? undefined : v)` in `incidentForm.schema.ts` (same normalization behavior, cleaner input/output typing), and (2) giving `useForm` its full 3 generics — `useForm<z.input<typeof updateIncidentFormSchema>, unknown, UpdateIncidentFormInput>` — so `TFieldValues` (raw) and `TTransformedValues` (parsed, used by `onSubmit`) are both explicit and match the resolver exactly. Verified via `tsc -b --force` (0 errors in both files) and `vitest run` (12/12 tests still pass).

## 3. Independent Resolver-type fixes in NCForm.tsx and QEACSection.tsx (D3)

- [x] 3.1 `NCForm.tsx`: root cause was `createNCSchema`'s `requiereIPER`/`documentosVinculados` fields using `.default(...)`, which (same class of bug as 2.2) makes the resolver's raw-input type diverge from its parsed-output type. Fixed by giving `useForm` explicit input/output generics: `useForm<z.input<typeof createNCSchema>, unknown, CreateNCInput>`. This also surfaced a real latent gap in `handleForzar` (line ~155), which called `mutateAsync(getValues())` directly, bypassing the resolver's defaulting — `getValues()`'s honest (now-correct) type doesn't guarantee `requiereIPER`/`documentosVinculados` are present. Fixed by explicitly replicating the same defaults used in `defaultValues` when building that payload, rather than casting around it.
- [x] 3.2 `QEACSection.tsx`: only the `createQEAccionSchema` call site (line ~161, the "agregar AC" modal) had errors — the other 3 (`cerrarQEAccionSchema`, `solicitarAjustePlazoACSchema`, `rechazarAjustePlazoACSchema`) were already clean. Root cause: `prioridad` uses `z.preprocess()`, same input/output split. Fixed with the same explicit-generics pattern as 3.1 — `useForm<z.input<typeof createQEAccionSchema>, unknown, CreateQEACInput>` — with **no schema change needed** here (unlike `IncidentForm.tsx` in 2.2, where `localId`/`zonaId` are bound via `<Controller>` + `field.value`, and the `unknown` input type leaked into that JSX usage — the schema-level `preprocess()→transform()` fix stays there; this file didn't need it since its `prioridad` field isn't Controller-bound).
- [x] 3.3 Run `npx tsc -b --force`: confirmed all `TS2719`/`TS2345` Resolver-pattern errors are gone from `NCForm.tsx` and `QEACSection.tsx`. Total error count: 82 → 71. `vitest run` on `IncidentForm.test.tsx`, `IncidentDetailPage.test.tsx`, `NCForm.test.tsx`, `QEACSection.test.tsx` — 24/24 pass.

## 4. Documento.rolesAutorizados narrowing (D4)

- [x] 4.1 Added `DocumentAuthorizedRole` to `src/types/documents.types.ts`, next to `DocConfidencialidad`.
- [x] 4.2 Changed `Documento.rolesAutorizados` from `UserRole[]` to `DocumentAuthorizedRole[]`; removed the now-unused `UserRole` import from that file.
- [x] 4.3 Added `DOCUMENT_AUTHORIZED_ROLES` (the single 6-role source) to `src/features/documents/constants.ts`; `documentForm.schema.ts`, `createDocument.schema.ts`, and `updateDocument.schema.ts` now build their local `userRoleEnum` from `z.enum(DOCUMENT_AUTHORIZED_ROLES)` instead of each redeclaring the literal list.
- [x] 4.4 Ran `npx tsc -b --force`: `useDocumentForm.ts`'s `TS2322` is gone. One new error surfaced in `documents.handlers.ts:183` (`(d.rolesAutorizados ?? []).includes(userRole as UserRole)` — `userRole` cast too wide for the narrower field), fixed by casting to `string[]` instead (matching the already-loose, untyped-Set convention used one line above for the `CONFIDENCIAL` check, rather than importing a specific literal type for a value that's just a raw URL query param). `documentPdf.ts`, `DocumentDetailHeader.tsx`, `DocumentActionPanel.tsx` all read `rolesAutorizados` but needed no changes (their usage — `.includes()`/spread into UI text — is compatible with the narrower type). Verified with `vitest run` on `useDocumentForm`/`DocumentForm`/`documents.handlers` tests — all pass except 2 pre-existing failures in `documents.handlers.test.ts` (arraybuffer/binary-download tests) that trace to an **unrelated, already-uncommitted WIP change in `src/lib/axios.ts`** (its response-envelope check now hard-rejects any non-JSON body, including binary downloads) — confirmed via isolated revert that this is 100% unrelated to this change and pre-dates this session; out of scope here, left untouched, and flagged to the user.
- [ ] 4.5 Update `openspec/specs/document-types/spec.md` main spec via `/opsx:sync` (or manually) once this change is applied, so the archived spec matches the new `DocumentAuthorizedRole` requirement.

## 5. vite.config.ts test block (D5)

- [x] 5.1 Changed `vite.config.ts`'s `import { defineConfig } from "vite"` to `import { defineConfig } from "vitest/config"`.
- [x] 5.2 Ran `npx tsc -b --force`: the `TS2769` on the `test` block is gone. Total: 70 → 69.
- [x] 5.3 Ran `npx vitest run` on `Pagination.test.tsx` — 6/6 pass, config loads correctly.

## 6. Remaining one-off fixes (D6-D8)

- [x] 6.1 `nonconformities.handlers.ts:245` — changed to `(nc as unknown as Record<string, unknown>)[campo]`, matching `quality-events.handlers.ts`'s existing pattern.
- [x] 6.2 `DocumentDetailPage.tsx:17` — no existing convention elsewhere in the codebase for this pattern (confirmed via search — this file is the only `JSX.Element` return-type usage anywhere). Fixed with `import type { JSX } from 'react'`.
- [x] 6.3 `DocumentForm.tsx:181` — confirmed genuinely dead, not a dropped check: the file's upload UI fully migrated to the ADD-02 model (`archivoOriginalFile`/`archivoDistribucion`, both rendered and wired), and no code path reads or renders `existingFileUrl` anywhere. Removed from `DocumentFormProps`, the component's destructuring, and the one caller (`DocumentFormPage.tsx:97`, which was still passing `existingFileUrl={documento?.archivoUrl}` into the unused prop).
- [x] 6.4 `src/types/documents.types.test.ts:68` — updated to `toEqualTypeOf<'Documento' | 'NoConformidad'>()`; also renamed the test from "narrows entidadTipo to the literal Documento" to "narrows entidadTipo to the Documento | NoConformidad union" since the old name was itself stale.
- [x] 6.5 `src/mocks/handlers/locales.handlers.test.ts:85` — the `call<T>()` test helper's parameter type assumed every wrapped promise looks like a raw axios response (`{data, status}`), but `crearLocal()` is a higher-level API function that already returns the unwrapped entity (`Promise<Local>`). Every call site in this file uses an explicit `call<T>(...)` generic already, so widening the parameter to `Promise<unknown>` (with an internal cast) doesn't affect inference anywhere — the helper's actual behavior (extracting `status`/`data` from either a resolved value or a caught `AxiosError`) is unchanged.
- [x] 6.6 `src/mocks/handlers/auth.handlers.test.ts` and `src/mocks/handlers/documents.handlers.test.ts` fixed separately (different root causes, not the same bug):
  - `auth.handlers.test.ts:38` — TS narrowed `fixture.lastLogin` to the literal `undefined` from an earlier direct assignment (line 28), not accounting for the mutation happening server-side across the intervening `await` — a real, known TS control-flow limitation, not a logic bug. Fixed with the double-cast TS itself suggests: `fixture.lastLogin as unknown as string`.
  - `documents.handlers.test.ts`'s `call<T>()` helper: **unlike `locales.handlers.test.ts`, every call site here omits the generic and relies on inference from the argument** — an initial `Promise<unknown>` fix (mirroring 6.5) broke that inference project-wide (5 new `TS18046`/`TS7006` errors at `.data` use sites). Corrected by typing the parameter as `Promise<AxiosResponse<T>>` (imported from `axios`) instead — this matches what every call site actually passes, preserves `T` inference everywhere, and resolves the original `headers` mismatch since `AxiosResponse['headers']` is now the parameter's real type rather than a hand-rolled `Record<string, string>`.

## 7. Final verification

- [x] 7.1 Ran `npx tsc -b --force`: every file in the proposal's Impact section is individually confirmed clean (checked file-by-file, not just the aggregate count). Total project-wide count: 82 → 58 (27 `TS2739` + 23 `TS2353` fixture errors, out of scope, + **8 newly-discovered stray errors in files neither this change nor `fix-tsc-test-fixture-errors` declared** — see note below).
- [x] 7.2 Ran `npx vitest run` (full suite): 13 files / 35 tests fail on the current tree. Verified via `git stash` that pure committed `HEAD` (no WIP, no changes from this session) has **68** failing tests / 17 failing files — strictly worse. This confirms none of the 35 current failures are regressions introduced by this change; they're pre-existing gaps in the repo's in-progress WIP (M7 offline/PWA, MultiEmpresa, Users module) that predate this session, including the specific `axios.ts` envelope-check bug identified during task 4.4.
- [x] 7.3 Confirmed: the ~50 fixture errors are present and unaffected (27 `TS2739` + 23 `TS2353`, matching the expected count exactly); no `User`-fixture file was touched by this change.

**Note — scope gap discovered during final verification, not covered by either OpenSpec change:** 8 errors exist in files neither this proposal's Impact list nor `fix-tsc-test-fixture-errors`'s Impact list names: `NCStatusBadge.test.tsx` (TS6133), `useLogin.test.tsx` (TS2322, User-shape — same class as Cause C), `PanelPendientesAreaWidget.test.tsx` (TS2345), `useAccionesRequeridas.test.ts` (TS2322 on a local `buildUser()` helper — User-shape, same class as Cause C, distinct from the `Documento.area` issue already scoped there), `IncidentQuickReportForm.test.tsx` (TS2571 ×2), `IncidentListPage.test.tsx` (TS6133), `QEHeaderSection.test.tsx` (TS2556). These are not in the original Cowork diagnosis either — they emerged from repo evolution since that snapshot. Flagged to the user rather than fixed unilaterally, since fixing them here would exceed this change's declared scope.
