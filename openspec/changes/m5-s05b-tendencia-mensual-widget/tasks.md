## 1. Dependencia nueva: recharts

- [x] 1.1 Instalar `recharts` en `shc-controldoc/package.json` (`npm install recharts`). Confirmar que no hay conflicto de peer dependencies con React 19.
- [x] 1.2 Correr `npm run build` antes de cualquier otro cambio (baseline) y anotar el tamaño de `dist/` para comparar después de agregar el widget (ver tarea 8.2).
  - Nota: `npm run build` (`tsc -b && vite build`) falla en el paso `tsc -b` por errores de TypeScript preexistentes en `master`, no relacionados a esta spec (matchers de `jest-dom`/vitest sin tipos, `vite.config.ts` con `test` no reconocido en `UserConfigExport`, y 2 errores sueltos más). Verificado corriendo el mismo `npm run build` en `master` sin ningún cambio de esta spec. Se usó `vite build` directo para obtener el baseline: `dist/assets/index-*.js` 1,325.15 kB (gzip 371.86 kB), CSS 43.02 kB (gzip 8.26 kB).

## 2. Fix de fixtures — horas trabajadas

- [x] 2.1 En `src/mocks/fixtures/horasTrabajadas.fixtures.ts`, extender `PERIODOS` de 6 a 12 entradas cronológicas ascendentes: `['2025-08', '2025-09', '2025-10', '2025-11', '2025-12', '2026-01', '2026-02', '2026-03', '2026-04', '2026-05', '2026-06', '2026-07']`. No tocar `horasParaAreaYMes` — al ser determinística por índice de array, los 6 meses nuevos generan valores automáticamente.
- [x] 2.2 Actualizar el comentario de cabecera del archivo (referencia a "6 meses") para reflejar la cobertura de 12 meses.

## 3. Fix de fixtures — fechas de QE (volumen mensual)

Aplicar exactamente los cambios de la tabla de `design.md` (decisión 6) en `src/mocks/fixtures/quality-events.fixtures.ts`. No modificar `numero`, `severidad`, `ciclo`, `origen`, `tipo`, `accionesCorrectivas` ni `auditTrail` de ningún registro salvo lo indicado.

- [x] 3.1 `qe-2026-006`: `fechaHoraReporte` de `'2026-01-20T15:00:00Z'` a `'2025-08-20T15:00:00Z'`.
- [x] 3.2 `qe-2026-011`: `fechaHoraReporte` de `'2026-04-15T10:00:00Z'` a `'2025-09-15T10:00:00Z'`.
- [x] 3.3 `qe-2026-006`: agregar `causaRaizFirmadaEn: '2025-08-25T10:00:00Z'`, `fechaCierre: '2025-08-29T10:00:00Z'`; cambiar `estado` de `'ABIERTO'` a `'CERRADO'`.
- [x] 3.4 `qe-2026-011`: agregar `causaRaizFirmadaEn: '2025-09-22T10:00:00Z'`, `fechaCierre: '2025-09-29T10:00:00Z'`; cambiar `estado` de `'ABIERTO'` a `'CERRADO'`.
- [x] 3.5 `qe-2026-002`: mover `causaRaizFirmadaEn` de `'2026-01-10T10:00:00Z'` a `'2025-12-22T10:00:00Z'`; agregar `fechaCierre: '2025-12-29T10:00:00Z'`; cambiar `estado` de `'EN_EJECUCION'` a `'CERRADO'`.
- [x] 3.6 `qe-2026-001`: agregar `causaRaizFirmadaEn: '2026-02-14T10:00:00Z'`, `fechaCierre: '2026-02-24T10:00:00Z'`; cambiar `estado` de `'EN_INVESTIGACION'` a `'CERRADO'`.
- [x] 3.7 `qe-2026-014`: agregar `causaRaizFirmadaEn: '2026-04-12T10:00:00Z'`, `fechaCierre: '2026-04-24T10:00:00Z'`; cambiar `estado` de `'ANALISIS_COMPLETADO'` a `'CERRADO'`.
- [x] 3.8 No tocar `qe-2026-013`, `qe-2026-005`, `qe-2026-017` (ver justificación en `design.md`).
- [x] 3.9 Correr la suite existente de fixtures/handlers (`quality-events.fixtures` si tiene test propio, `dashboard.handlers.test.ts`, cualquier test que dependa de conteos por `estado` — ej. `QEPorEstadoWidget.test.tsx`, `dashboardAccess.test.tsx`) y confirmar que ningún test que asuma los conteos anteriores por `estado` se rompe por los 6 cambios de estado (`ABIERTO` 3→1, `EN_EJECUCION` 2→1, `EN_INVESTIGACION` 3→2, `ANALISIS_COMPLETADO` 2→1, `CERRADO` 3→7). Ajustar cualquier assertion de conteo fijo que dependa del valor anterior.
  - Hallazgo: además de conteos por estado, los cambios de 3.1–3.7 rompieron 4 tests que usaban `qe-2026-001`/`qe-2026-002` como fixtures "editables" para probar mutaciones (PATCH severidad/mineral, transición de estado) — al pasar ambos a `CERRADO`, dejaron de ser válidos para esos casos. Se reapuntaron a otros QE del seed que preservan la precondición original de cada test: `useEditarSeveridad.test.ts` → `qe-2026-012` (caso "valid request") y `qe-2026-018` (caso "flags notification"); `useEditarMineral.test.ts` → `qe-2026-019` (caso "valid request"); `useTransitionQEStatus.test.ts` → `qe-2026-007` (caso RN-QE-002, QE sin `causaRaizFirmadaEn`). Suite completa verificada: solo quedan las 4 fallas preexistentes en `master` (`DeadlineBadge.test.tsx`, `Pagination.test.tsx`, `useNCList.test.ts`, `qualityEventCreate.schema.test.ts` — no relacionadas a esta spec).
- [x] 3.10 Verificar con un script/test temporal (o `console.log` en un test) que `tendenciaMensualVolumen.abiertos` no da `0` en ninguno de los 12 meses y que `cerrados` no da `0` en más de 2 de los 12 meses, antes de continuar.
  - Resultado: `abiertos` no da `0` en ningún mes. `cerrados` da `0` en exactamente 2 de los 12 meses (`2025-10`, `2026-01`) — dentro del límite permitido, documentado como gap aceptado (ningún registro restante tenía un `causaRaizFirmadaEn` narrativamente compatible con cerrar en esos 2 meses sin contradecir otra fecha ya fijada).

## 4. Fix de fixtures — KPI-05 histórico (mayor riesgo, verificar en código)

- [x] 4.1 Con los cambios de la sección 3 ya aplicados, ejecutar `calcularKpi05(qes, ncs, periodo)` para cada uno de los 12 meses (test temporal o script) y anotar en qué meses da `0`.
  - Resultado inicial (antes de 4.2): `0` en 9 de los 12 meses; solo `2026-02` (100), `2026-04` (100) y `2026-07` (50) tenían datos.
- [x] 4.2 Para los meses en `0`, revisar si alguna AC seedada (`qeAccionesCorrectivas`/`ncAccionesCorrectivas`) con `estado: 'CERRADA'` puede recibir/ajustar la fecha de verificación de su QE/NC padre (`fechaVerificacionRealizada`/`fechaVerificacion`) para caer en ese mes, sin romper las restricciones de `quality-event-msw-fixtures`/`nc-msw-fixtures` (mínimos de cobertura de enum, etc.). Priorizar mover fechas existentes de padres que ya tienen `resultadoVerificacion` (`qe-2026-004`, `qe-2026-010`, `qe-2026-017` y sus equivalentes NC) antes que introducir un nuevo padre verificado.
  - Se movió `qe-2026-017.fechaVerificacionRealizada` de `'2026-07-12T10:00:00Z'` a `'2026-05-10T10:00:00Z'` — esta fecha además corrige una inconsistencia ya existente en el fixture: su propio `auditTrail` (`aud-qe-017-4`, `CAMPO_EDITADO resultadoVerificacion`) ya registraba ese cambio en `2026-05-10`, antes de la fecha de `REABIERTO` (`2026-05-15`); el campo `fechaVerificacionRealizada` no estaba alineado con su propio historial. Resultado: separa el punto de `2026-07` (que antes combinaba `qe-2026-004` EFECTIVO + `qe-2026-017` NO_EFECTIVO = 50%) en dos meses distintos, `2026-05` (0%, solo NO_EFECTIVO) y `2026-07` (100%, solo `qe-2026-004`).
  - `qe-2026-010` (nombrado explícitamente en la tarea): tiene `resultadoVerificacion: 'EFECTIVO'` pero **no tiene ninguna AC `CERRADA` seedada** (no aparece en `qeAccionesCorrectivas`, `accionesCorrectivas: []`) — `calcularKpi05` exige una AC `CERRADA` para contar, así que no puede contribuir sin fabricar una AC nueva. Se descartó por violar el criterio de "no forzar un valor artificial sin respaldo narrativo".
  - No se encontraron equivalentes NC adicionales candidatos a mover: solo `nc-004` (`2026-02`) y `nc-012` (`2026-04`) tienen `resultadoVerificacion` + AC `CERRADA`, y ambos ya caían en meses distintos y vacíos — no había necesidad ni justificación narrativa para moverlos.
- [x] 4.3 Documentar en el PR/commit qué meses de KPI-05 histórico quedan en `0` de forma justificada (ningún AC verificado ese mes en la realidad narrativa del seed) tras el mejor esfuerzo — no forzar un valor artificial sin respaldo narrativo.
  - Resultado final: KPI-05 histórico tiene datos en 4 de los 12 meses (`2026-02`=100, `2026-04`=100, `2026-05`=0 — verificación NO_EFECTIVO real, no "sin datos" —, `2026-07`=100). Los 8 meses restantes (`2025-08`, `2025-09`, `2025-10`, `2025-11`, `2025-12`, `2026-01`, `2026-03`, `2026-06`) quedan en `0` por ausencia real de ACs verificadas ese mes en el seed — el único padre adicional con `resultadoVerificacion` sin AC `CERRADA` (`qe-2026-010`) no puede contribuir sin fabricar datos. Se documenta como gap aceptado según el criterio de `design.md` (Riesgos/Trade-offs) — no hay umbral mínimo de cobertura definido para KPI-05 histórico, a diferencia de `tendenciaMensualVolumen.cerrados`.

## 5. Tipos (`dashboard-types`)

- [x] 5.1 En `src/features/dashboard/types/dashboardData.types.ts`, agregar `TendenciaMensualVolumenEntry` (`{ periodo: string; abiertos: number; cerrados: number }`) y `TendenciaMensualKpiEntry` (`{ periodo: string; valor: number }`).
- [x] 5.2 En `JefeCalidadDashboardData`, eliminar `tendenciaMensualCierres` y agregar `tendenciaMensualVolumen: TendenciaMensualVolumenEntry[]` y `tendenciaMensualKpis: Record<'KPI-01' | 'KPI-04' | 'KPI-05', TendenciaMensualKpiEntry[]>`.

## 6. MSW handler (`dashboard-msw-handlers`)

- [x] 6.1 En `src/mocks/handlers/dashboard.handlers.ts`, reemplazar `buildTendenciaMensualCierres` por `buildTendenciaMensualVolumen(qes: QualityEvent[]): TendenciaMensualVolumenEntry[]`, iterando `ultimosMeses(12)` y contando por mes `abiertos` (`fechaHoraReporte` en rango) y `cerrados` (`fechaCierre` en rango, sin filtrar por `estado` — mismo criterio que la función que reemplaza).
- [x] 6.2 Agregar `buildTendenciaMensualKpis(qes, ncs, incidentes): JefeCalidadDashboardData['tendenciaMensualKpis']`, invocando `calcularKpi01(qes, periodo)`, `calcularKpi04(incidentes, periodo)`, `calcularKpi05(qes, ncs, periodo)` para cada uno de `ultimosMeses(12)`.
- [x] 6.3 En `buildJefeCalidadData`, reemplazar `tendenciaMensualCierres: buildTendenciaMensualCierres(qes)` por `tendenciaMensualVolumen: buildTendenciaMensualVolumen(qes)` y `tendenciaMensualKpis: buildTendenciaMensualKpis(qes, ncs, incidentes)` (agregar `incidentes` como variable local si no está ya destructurada en `buildJefeCalidadData`).
- [x] 6.4 Actualizar `dashboard.handlers.test.ts`: casos para `tendenciaMensualVolumen` con 12 entradas ordenadas cronológicamente, `abiertos`/`cerrados` calculados correctamente para al menos un mes con datos conocidos; casos para `tendenciaMensualKpis` con exactamente las 3 claves `KPI-01`/`KPI-04`/`KPI-05` y valores que coinciden con invocar `calcularKpi01`/`04`/`05` directamente para el mismo `periodo`; caso confirmando que `JEFE_CONTROL_DOCUMENTARIO` recibe la misma tendencia que `JEFE_CALIDAD_SYST`.

## 7. i18n

- [x] 7.1 Agregar namespace `dashboard.jefeCalidad.tendencia` en `src/i18n/es-PE.json` y `en-US.json`: `title`, `rango.label`, `rango.opciones.3`/`6`/`12`, `volumen.title`, `volumen.abiertos`, `volumen.cerrados`, `kpis.title`, `kpis.kpi01`, `kpis.kpi04`, `kpis.kpi05`.

## 8. Componente del widget (`dashboard-trend-widget`)

- [x] 8.1 Crear `src/features/dashboard/components/TendenciaMensualWidget.tsx` (+ test): recibe `tendenciaMensualVolumen: TendenciaMensualVolumenEntry[]` y `tendenciaMensualKpis: JefeCalidadDashboardData['tendenciaMensualKpis']`; estado local `rango: 3 | 6 | 12` (default `6`) vía `useState`; deriva los datos recortados con `useMemo` (`array.slice(-rango)`), nunca `useEffect`.
- [x] 8.2 Selector de rango: 3 botones/toggle (`3`/`6`/`12` meses) con `aria-label` y estado activo visualmente distinguible (clases con variante `dark:`).
- [x] 8.3 Gráfico de volumen: `LineChart` de recharts con 2 `Line` (`abiertos` color `coral` `#cc785c`, `cerrados` color `teal` `#5db8a6`), eje X formateado con `Intl.DateTimeFormat(locale, { month: 'short', year: 'numeric' })` sobre el `periodo` (`'YYYY-MM'` parseado a `Date`), leyenda con `t('namespace:clave')`.
- [x] 8.4 Gráfico de KPIs: `LineChart` con 3 `Line` (KPI-01 `coral`, KPI-04 `amber` `#e8a55a`, KPI-05 `teal`), mismo formato de eje X, sin color por semáforo.
- [x] 8.5 Estado vacío/carga: si `tendenciaMensualVolumen`/`tendenciaMensualKpis` no llegan (loading), reusar el mismo patrón de esqueleto que los otros widgets de `JefeCalidadDashboard`.
  - El widget no maneja su propio loading: igual que `KpiGridWidget`/`QEPorEstadoWidget`/`ACsPorVencerWidget`, `JefeCalidadDashboard` no lo monta hasta que `useDashboardSummary()` resuelve (ver `WidgetSkeleton` en la página, ahora con 4 grupos en vez de 3).
- [x] 8.6 Verificar Light/Dark mode manualmente (o snapshot) — colores de línea legibles en ambos temas sin cambiar de valor por tema.
  - Verificado en navegador (Chromium headless vía Playwright): ambos gráficos legibles y con buen contraste en Light y Dark mode; ver evidencia de la tarea 10.1.
  - Nota técnica: se agregó un polyfill de `ResizeObserver` en `src/test-setup.ts` (jsdom no lo implementa) — necesario para que `ResponsiveContainer` de recharts renderice en tests; sigue el mismo patrón que el polyfill de `matchMedia` ya existente en ese archivo.

## 9. Wiring en la página

- [x] 9.1 En `src/features/dashboard/pages/JefeCalidadDashboard.tsx`, agregar `<TendenciaMensualWidget tendenciaMensualVolumen={data.tendenciaMensualVolumen} tendenciaMensualKpis={data.tendenciaMensualKpis} />` junto a los 3 widgets existentes.
- [x] 9.2 Actualizar `JefeCalidadDashboard.test.tsx` con un caso que confirme que `TendenciaMensualWidget` se renderiza con los datos de `useDashboardSummary()`.

## 10. Verificación cruzada

- [x] 10.1 Levantar la app con `VITE_ENABLE_MSW=true`, iniciar sesión como `JEFE_CALIDAD_SYST` y como `JEFE_CONTROL_DOCUMENTARIO`, y verificar en navegador: ambos gráficos renderizan con datos distintos de cero en la mayoría de los 12 meses, el selector de rango cambia ambos gráficos sin nueva petición de red (Network tab), y el widget se ve correctamente en Light y Dark mode.
  - Verificado con Playwright (Chromium headless) contra `npm run dev`: login como ambos roles, capturas en Light y Dark, y confirmación instrumentada de que hacer clic en el selector de rango (`6`→`12` meses) dispara **0** peticiones nuevas a `/api/dashboard/summary`. `JEFE_CONTROL_DOCUMENTARIO` recibe visualmente la misma tendencia que `JEFE_CALIDAD_SYST`.
- [x] 10.2 Correr `npm run build` y comparar el tamaño de `dist/` contra el baseline de la tarea 1.2; documentar el delta (no hay un umbral de fallo definido, pero un incremento inesperadamente grande — ej. >500KB — debe investigarse antes de cerrar la spec).
  - `tsc -b` sigue fallando por el mismo motivo preexistente que en el baseline (ver nota de 1.2); se comparó con `vite build` directo en ambos casos. Después: `dist/assets/index-*.js` 1,693.35 kB (gzip 479.26 kB), CSS 43.10 kB (gzip 8.29 kB). Delta: **+368.2 kB raw / +107.4 kB gzip** — bajo el umbral de investigación (500KB) y esperable para la primera librería de gráficos del proyecto (recharts trae `d3-shape`/`d3-scale`/`d3-interpolate`).
- [x] 10.3 Correr `npm run typecheck`, `npm run lint` y la suite completa de tests (`npm test`) antes de dar la spec por completa.
  - No existe script `npm run typecheck` en `package.json` (solo `dev`/`build`/`lint`/`preview`) — se usó `npx tsc -p tsconfig.app.json --noEmit` en su lugar. Todos los errores reportados están en archivos no tocados por esta spec o son el mismo problema preexistente de tipos de `jest-dom` (confirmado también en el baseline de la tarea 1.2); ningún archivo nuevo/modificado por esta spec introduce un error de tipos distinto a ese patrón conocido.
  - `npm run lint` (`eslint .`): 39 problemas preexistentes, todos en archivos no tocados por esta spec (confirmado por archivo). Los archivos nuevos/modificados de esta spec lintean sin errores ni warnings.
  - `npx vitest run`: 88/92 archivos de test pasan (721/724 tests). Las 4 fallas restantes son las mismas 4 preexistentes en `master` antes de este cambio (`DeadlineBadge.test.tsx`, `Pagination.test.tsx`, `useNCList.test.ts`, `qualityEventCreate.schema.test.ts`), no relacionadas a esta spec.

## 11. Correcciones post-verificación en navegador (bloqueantes antes de archivar)

- [x] 11.1 **Interpolación engañosa en el gráfico de KPIs**: cambiar `type="monotone"` a `type="linear"` en las 3 `Line` de KPI-01/04/05 (`TendenciaMensualWidget.tsx`) — con denominadores mensuales pequeños, `monotone` dibujaba curvas suaves que sugerían valores intermedios inexistentes entre dos puntos reales. Se evaluó también el gráfico de volumen (Abiertos/Cerrados): sus conteos son igualmente discretos (no continuos), así que se aplicó el mismo criterio por defecto y sus 2 `Line` también pasan a `type="linear"`.
- [x] 11.2 **Tooltip de recharts sin dark mode**: recharts renderiza el contenido del `<Tooltip>` en un `<div>` HTML normal (no SVG), así que se reemplazó el `contentStyle`/default por un componente `ChartTooltip` propio con clases Tailwind (`bg-canvas dark:bg-surface-dark-elevated`, `border-hairline dark:border-hairline/20`, `text-body-strong dark:text-on-dark`) en ambos gráficos. Verificado en navegador (Playwright, Chromium headless) en Light y Dark: fondo y texto legibles en ambos temas. De paso se redondean los valores numéricos del tooltip a 1 decimal (antes mostraba floats sin redondear, ej. `66.66666666666666`).
- [x] 11.3 **Fixtures con muestra mensual insuficiente para KPI-01/05**: los 21 QE originales dejaban la mayoría de los 12 meses con denominadores de 1-4 casos, forzando valores binarios (0%/100%) mes a mes. Se agregó un generador determinístico (`buildTendenciaSeedQE` en `quality-events.fixtures.ts`) que crea ~50 QE sintéticos adicionales (`qe-tendencia-N`, sin narrativa cruzada con los 21 originales) distribuidos en los 12 meses, elevando el denominador de KPI-01 a exactamente 5 casos/mes (mezcla ~2/3 en-plazo, 1/3 fuera de plazo) y el de KPI-05 a 1-3 casos/mes (mezcla EFECTIVO/NO_EFECTIVO) vía una AC `CERRADA` seedeada por cada QE que además queda `VERIFICADO`. Se excluyó deliberadamente `'Almacén Norte'/'Almacén Sur'/'Galpón B'/'Galpón C'` de la rotación de áreas de estos QE sintéticos porque `useDashboardSummary.test.ts` verifica un conteo exacto de QE por Supervisor sobre esas áreas — cualquier QE ahí lo hubiera roto (se detectó y corrigió durante esta tarea). También se agregaron 2 incidentes sintéticos con `huboLesionados: true` (`inc-019` en 2025-10, `inc-020` en 2026-05, `incidents.fixtures.ts`) para que KPI-04 histórico tenga 5 de 12 meses no-cero en vez de 3, sin tocar el valor del mes actual (2026-07) ni la serie curada Ene-Mar (21.55→20.73→19.97, sin cambios). Verificado con un test temporal (`__tmp_stats.test.ts`, eliminado tras el análisis) que KPI-01 pasa de denominadores 0-3 a exactamente 5 en los 12 meses, y KPI-05 de 8 meses en `0` a solo 2 meses en `0`.
- [x] 11.4 Regresión completa tras 11.1–11.3: `npx tsc -p tsconfig.app.json --noEmit` (sin errores nuevos en archivos tocados), `npx eslint` sobre los 3 archivos modificados (sin errores), `npx vitest run` (721/724 — mismas 4 fallas preexistentes de la tarea 10.3, ninguna nueva; se detectó y corrigió una regresión real en `useDashboardSummary.test.ts` durante el proceso, ver 11.3), `npx vite build` (bundle 1,697.99 kB / gzip 480.58 kB — variación despreciable vs. el delta ya documentado en 10.2). Verificación visual en navegador (Playwright, Chromium headless, login como `jefe.calidad@shac.pe`) confirma: líneas rectas entre puntos (sin curvas), tooltip legible en Light y Dark, y KPI-01/05 con valores intermedios reales en la mayoría de los 12 meses del rango.
