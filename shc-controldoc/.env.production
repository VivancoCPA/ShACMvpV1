# VITE_API_BASE_URL: pendiente hasta decidir el hosting/dominio real de
# producción (ver openspec/changes/cutover-auth/design.md, Open Questions).
# No dejar vacío en un deploy real: un build de producción con MSW activo
# serviría datos ficticios en vez del backend real.
VITE_API_BASE_URL=
VITE_ENABLE_MSW=false
